## Codebase Patterns
- Every `#include "..."` directive MUST match the exact filename casing on disk (case-sensitive for Linux).
- Use `python3 verify_includes.py` to check for casing issues.
- Use global string utility functions from `meos_util.h` for conversions (widen, narrow, toUTF8, fromUTF8, recodeToWide, recodeToNarrow).
- Use standard C++ types (uint32_t, uint8_t, bool, etc.) in domain files and include <cstdint> in headers.
- Be careful with BOOL in SQL strings; it might be a database-specific type (like in MySQL/SQLite) that should remain as is.
- Use CMake and vcpkg for cross-platform builds. The `CMakeLists.txt` in the root builds the project on Linux.
- GCC on Linux is stricter than MSVC about incomplete types in standard containers; always provide a definition for classes used in `std::vector` even if they are just stubs.

## 2026-03-05 - US-P0a
- Fixed 147 include case mismatches in the `code/` directory.
- Created `verify_includes.py` for automated verification of include casing.
- Updated `AGENTS.md` and `code/AGENTS.md` with the new casing convention.
- Updated `.gemini/skills/migration/SKILL.md` with migration learnings.
- **Learnings for future iterations:**
  - `stdafx.h` is the most common mismatch (should be `StdAfx.h`).
  - Many domain headers like `localizer.h`, `meosexception.h` were inconsistently cased in includes.
  - Using a script to discover all files and then mapping them case-insensitively is the most robust way to find these issues.
  - Windows doesn't care about casing, but Linux does. Fixing this early is crucial for cross-platform development.
---

## 2026-03-05 - US-P0b
- Extracted widen(), narrow(), toUTF8(), fromUTF8(), recodeToWide(), recodeToNarrow() from gdioutput to meos_util.h/cpp.
- Updated gdioutput.cpp to use the new global functions as thin wrappers.
- Performed global replacement of static and member calls (e.g., gdi.widen() -> widen()) in all code files using 'generalist'.
- Removed gdioutput.h dependency from machinecontainer.cpp and infoserver.cpp.
- Updated AGENTS.md and migration skill with the new string utility conventions.
- **Learnings for future iterations:**
  - gdioutput is heavily coupled to almost every part of the legacy codebase, including domain entities.
  - The string utility functions were the easiest to decouple; other gdioutput functions (ask, alert, addString) require a more comprehensive interface-based approach.
  - Using 'generalist' for global batch replacements is efficient but requires careful verification of headers in each file afterwards.
---

## 2026-03-05 - US-P0c
- Replaced Win32-specific string functions (_wtoi, _itow_s, swprintf_s, sprintf_s, _stricmp, _wcsicmp, lstrcmpi, _wtof, _wtoi64) with standard C++ or portable equivalents in domain code.
- Added portable case-insensitive comparison wrappers (compareStringIgnoreCase) to meos_util.h/cpp.
- Updated StdAfx.h to include <cstdio> and <cwchar>.
- Refactored meos_util.cpp to use standard string functions.
- Fixed 100+ occurrences across all domain files (o*.cpp/h, generalresult.cpp/h, metalist.cpp/h, datadefiners.h).
- **Learnings for future iterations:**
  - Case-insensitive string comparison is one of the most common Win32-specific patterns (stricmp/wcsicmp/lstrcmpi). A central wrapper is better than manual replacement.
  - swprintf/snprintf are standard but have slightly different behavior/signatures than swprintf_s/sprintf_s in MSVC (especially regarding buffer size inference). Always specify the size explicitly for portability.
  - Some legacy files use ISO-8859 encoding, which can cause issues with UTF-8 based tools.
  - Domain files are mostly o*.cpp/h but also include datadefiners.h, generalresult.cpp, and metalist.cpp.
---

## 2026-03-05 - US-P0d
- Replaced Win32 types (DWORD, BOOL, WORD, BYTE, LPWSTR) with standard C++ types (uint32_t, bool, uint16_t, uint8_t, wchar_t*) in domain files.
- Added #include <cstdint> to domain headers using these types (oEvent.h, oCourse.h, oControl.h, oClub.h, meos_util.h, onlineinput.h, MeosSQL.h).
- Excluded BOOL usage in SQL query strings in MeosSQL.cpp to prevent breaking database schema.
- Verified removal of Win32 types across domain files.
- **Learnings for future iterations:**
  - Standard types like uint32_t/uint8_t are more portable but require explicit <cstdint> inclusion in headers.
  - Be careful with BOOL in SQL strings; it might be a database-specific type (like in MySQL/SQLite) that should remain as is.
  - sed -i is powerful for bulk replacement but always use \b for word boundaries.
  - Some files that were not in the initial "domain" list (like meos_util.cpp, xmlparser.cpp) should also be cleaned up as they are foundational.
---

## 2026-03-05 - US-P0e
- Replaced Win32-specific path splitting functions (_wsplitpath_s) with std::filesystem::path in image.cpp, meos.cpp, oEvent.cpp, and HTMLWriter.cpp.
- Replaced GetFileAttributes with std::filesystem::exists in meos_util.cpp.
- Fixed remaining hardcoded backslash in TabList.cpp.
- Updated code/AGENTS.md with new path normalization guidelines.
- **Learnings for future iterations:**
  - std::filesystem::path::stem() is a direct replacement for _wsplitpath_s(..., nameid, ...) when combined with manual dot removal loops.
  - _wsplitpath_s is often used just to get the filename or parent directory; std::filesystem::path::filename() and parent_path() are cleaner.
  - When replacing _wsplitpath_s's 'dir' component with parent_path(), be aware that parent_path() might not include a trailing slash, which some legacy concatenation logic might expect.
  - StdAfx.h 'using std::filesystem::path' alias makes path usage very concise.
---

## 2026-03-05 - US-P0f
- Decoupled `oEvent` from `TabList`, `TabAuto`, and `TabSI` UI classes.
- Added `std::function` callbacks (`baseButtonsCallback`, `tabAutoKillMachinesCallback`, `setSubSecondModeCallback`) to `oEvent` class.
- Replaced direct static calls to `TabList`, `TabAuto`, and `TabSI` in `oEvent.cpp` with these callbacks.
- Removed UI-related includes from `oEvent.cpp`.
- Registered the callbacks in `meos.cpp` during application initialization.
- Added `UI Decoupling` pattern documentation to `code/AGENTS.md`.
- **Learnings for future iterations:**
  - `std::function` is a robust way to decouple domain logic from UI without introducing complex interface inheritance.
  - UI classes often use static pointers to their singleton instances; these can be safely accessed via callbacks registered in the main application loop.
  - Decoupling `oEvent` (the aggregate root) is the most critical step for making the domain model portable and testable.
  - Lambdas in `meos.cpp` require `#include <functional>`.
---

## 2026-03-06 - US-001 - CMake build system
- Implemented top-level `CMakeLists.txt` that builds a minimal executable `meos` on Linux.
- Integrated vcpkg in manifest mode with `vcpkg.json` declaring dependencies (libharu, libpng, zlib, nlohmann-json, restbed, sqlite3, gtest, minizip).
- Configured precompiled headers using `code/StdAfx.h` for build performance.
- Created `code/meos_stubs.cpp` to provide minimal string and utility stubs to decouple the minimal build from the heavily interconnected legacy codebase.
- Fixed several Win32 shimming issues in `code/win_types.h`:
  - Refactored `OffsetRect` from a buggy macro to an inline function.
  - Added missing typedefs: `INT`, `LPSIZE`, `LPOPENFILENAME`, `LPBROWSEINFO`.
  - Defined `GDIImplFontEnum` and `GDIImplFontSet` as empty classes to satisfy `std::vector` destructor requirements on GCC.
  - Wrapped `IDD_SPLASH` in `#ifndef` to avoid redefinition conflicts.
- Verified that the executable builds, links, and runs on Linux.
- **Learnings for future iterations:**
  - `libmysql` is difficult to build from source via vcpkg in some Linux environments due to heavy dependencies and build tool requirements; consider stubbing it or using a simpler alternative for early migration stages.
  - Standard C++ `std::wstring_convert` (though deprecated) is useful for implementing `widen`/`narrow` stubs quickly.
  - The legacy codebase is extremely coupled; a "minimal" build requires either bringing in dozens of files or providing strategic stubs. A dedicated `meos_stubs.cpp` is effective for this.
  - GCC on Linux is stricter than MSVC about incomplete types in standard containers; always provide a definition for classes used in `std::vector` even if they are just stubs.
---

## US-001 — COMPLETED (57m18s)

## 2026-03-06 - US-002 - Modular source layout
- Reorganized 'code/' directory into logical modules in 'src/': 'app', 'domain', 'db', 'net', 'io', 'util', and 'ui'.
- Updated root 'CMakeLists.txt' to define static libraries for each module.
- Configured common include paths in CMake so legacy bare includes (e.g., '#include "oBase.h"') still work.
- Moved resources (images, localization) to 'src/app/resources' and 'src/app/lang'.
- Established 'src/AGENTS.md' with module definitions and updated root 'AGENTS.md'.
- Verified the build on Linux using the new modular structure.
- **Learnings for future iterations:**
  - Categorizing ~190 files into modules is best done by identifying key aggregate roots (domain) and then their dependencies.
  - CMake static libraries require at least one source file; dummy 'module_module.cpp' files are useful placeholders while modules are being populated with portable code.
  - Legacy MSBuild/Win32 projects often have a flat structure; moving to a modular layout early helps identify and fix circular dependencies.
  - Precompiled headers ('StdAfx.h') need special care when moving to ensure they are still applied to all modules.
---

## US-002 (27m12s)
## 2026-03-06 15:30 - US-015
- Implemented test infrastructure for C++ (Google Test) and frontend (Vitest).
- Added 'tests/' directory with smoke tests and module-specific tests.
- Configured CMake to enable testing and add test targets.
- Added code coverage support in CMake via 'COVERAGE' option.
- Initialized 'src/ui/web' with TypeScript and Vitest configuration.
- Fixed 'util' module build issues by adding 'meos_util.cpp' and required stubs to 'meos_stubs.cpp'.
- Updated '.gitignore' to exclude build artifacts and node_modules.
- **Learnings for future iterations:**
  - 'meos_util.cpp' depends on 'Localizer' (via 'lang' global), creating a circular dependency if not stubbed or moved.
  - 'oWordList' and 'oWordIndexHash' need stubs if they are used in headers that 'util' depends on.
  - GCC/Linux is stricter about vtables; all virtual methods of a class must be implemented (even if as stubs) to avoid linker errors.
  - Vitest with jsdom and testing-library requires specific configuration for ESM/TypeScript compatibility.
---

## US-015 (30m12s)
