# Ralph Progress Log
Started: sön  8 mar 2026 00:14:24 CET
---

## Codebase Patterns
- VCPKG_ROOT is at ~/vcpkg on this machine (not /usr/local/share/vcpkg)
- CMakePresets.json uses `$env{VCPKG_ROOT}` for toolchain file path — set VCPKG_ROOT before invoking cmake
- Use cmake preset version 6 (cmake 3.28 available)
- g++ 13.3 and ninja are available as build tools
- Placeholder .cpp files are needed in library targets (CMake won't create a target with no sources)
- Use `eslint.config.js` (flat config) for ESLint 9+
- Prettier should be integrated with ESLint via `eslint-plugin-prettier`

---

## 2026-03-08 - US-001
- Implemented CMake build system with vcpkg integration
- Files created:
  - CMakeLists.txt (top-level with project declaration, add_subdirectory, PCH, testing option)
  - CMakePresets.json (default=Debug, release=Release configure/build presets)
  - vcpkg.json (manifest with empty dependencies)
  - src/main.cpp (minimal int main)
  - src/domain/CMakeLists.txt + placeholder.cpp (static library)
  - src/util/CMakeLists.txt + placeholder.cpp (static library)
  - .gitignore updated with build/, out/, vcpkg_installed/, CMakeUserPresets.json
- **Learnings for future iterations:**
  - vcpkg is installed at ~/vcpkg, not at /usr/local/share/vcpkg — the earlier `ls` output was misleading due to permissions
  - CMakePresets.json version 6 works with cmake 3.28
  - Placeholder .cpp files with just comments are sufficient to satisfy CMake source requirements
  - The build produces static libraries (libdomain.a, libutil.a) and a meos executable
  - No need for find_package() calls at this stage since vcpkg.json has no dependencies yet
---

## 2026-03-08 - US-002
- Implemented C++ test infrastructure with Google Test
- Files created/modified:
  - vcpkg.json (added gtest)
  - CMakeLists.txt (added add_subdirectory(tests))
  - tests/CMakeLists.txt (find_package GTest and subdirectories)
  - tests/domain/CMakeLists.txt + smoke_test.cpp
  - tests/util/CMakeLists.txt + smoke_test.cpp
- **Learnings for future iterations:**
  - GTest::gtest_main is the easiest way to get a main() for tests without writing one.
  - find_package(GTest REQUIRED) should be in the tests/CMakeLists.txt or top-level.
  - ctest --test-dir build is useful when building in a separate directory.
---

## US-002 (4m58s)

## 2026-03-08 17:17:48 - US-003
- Implemented Frontend Test Infrastructure with Vitest
- Files created:
  - src/ui/web/package.json (dependencies and scripts)
  - src/ui/web/tsconfig.json + tsconfig.node.json
  - src/ui/web/vite.config.ts + vitest.config.ts
  - src/ui/web/src/setupTests.ts (jest-dom setup)
  - src/ui/web/src/App.tsx + main.tsx + index.html
  - src/ui/web/src/__tests__/smoke.test.tsx
- **Learnings for future iterations:**
  - npm create vite@latest is interactive and can hang; manual file creation or --no-interactive flags are better for automation.
  - React 17+ doesn't need 'import React from "react"' in every JSX file; keeping it may trigger unused variable errors in strict TS mode.
  - v8 is a reliable coverage provider for Vitest.
  - jsdom is necessary for testing React components that interact with the DOM.
---

## US-003 (14m44s)

## 2026-03-08 - US-004
...
---

## 2026-03-08 - US-005
- Implemented CI/CD pipeline for C++ Build and Test via GitHub Actions
- Files created:
  - .github/workflows/cpp.yml
- **Learnings for future iterations:**
  - `lukka/run-vcpkg` is the most reliable way to integrate vcpkg with GitHub Actions.
  - Using `ninja` for builds in CI is generally faster.
  - On GitHub-hosted runners, `vcpkg` is often pre-installed, but `lukka/run-vcpkg` ensures it's in a known state.
  - `clang-tidy` requires `compile_commands.json`, which is generated by CMake with `CMAKE_EXPORT_COMPILE_COMMANDS=ON`.
---
