## Codebase Patterns
- Use CMake with presets (`CMakePresets.json`) for building.
- Dependencies are managed via `vcpkg` and `vcpkg.json`.
- The modern source code is located in `src/`.
- Executable name is `meos`.

## 2026-03-03 23:15 - US-001
- Implemented CMake build system with vcpkg integration.
- Created `vcpkg.json` with `nlohmann-json` and `sqlite3` dependencies.
- Created `CMakeLists.txt` and `CMakePresets.json`.
- Added a minimal `src/main.cpp` that verifies dependencies.
- **Learnings for future iterations:**
  - vcpkg is installed at `/home/adam.georgsson@fnox.it/vcpkg/`.
  - Use `cmake --preset default` to configure and `cmake --build --preset default` to build.
  - The compiler found is GCC 13.3.0.
---

## US-001 (4m22s)

## 2026-03-03 23:30 - US-002
- Created src/ subdirectories for app, domain, net, db, util, io.
- Added CMakeLists.txt to each subdirectory defining static libraries.
- Linked all modules to the main meos executable.
- Verified bare header include works within modules.
- Updated AGENTS.md and created src/AGENTS.md.
- **Learnings for future iterations:**
  - Bare #include within a module works by adding target_include_directories(target PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}).
  - CMake static libraries require at least one source file (e.g., app_dummy.cpp).
---


## US-002 (3m01s)

## 2026-03-03 22:50 - US-003
- Integrated Google Test via vcpkg.
- Configured CMake to enable testing and added a tests/ directory.
- Created tests/CMakeLists.txt and tests/sample_test.cpp.
- Added testPresets to CMakePresets.json.
- Updated root AGENTS.md and created tests/AGENTS.md with testing documentation.
- **Learnings for future iterations:**
  - Remember to add  and link  and .
  - Added  to  to allow running Test project /home/adam.georgsson@fnox.it/workspace/meos/build
    Start 1: unit_tests
1/1 Test #1: unit_tests .......................   Passed    0.00 sec

100% tests passed, 0 tests failed out of 1

Total Test time (real) =   0.00 sec.
  - Tests require  in the top-level CMakeLists.txt.
---

## US-003 (3m22s)

## 2026-03-03 22:50 - US-003
- Integrated Google Test via vcpkg.
- Configured CMake to enable testing and added a tests/ directory.
- Created tests/CMakeLists.txt and tests/sample_test.cpp.
- Added testPresets to CMakePresets.json.
- Updated root AGENTS.md and created tests/AGENTS.md with testing documentation.
- **Learnings for future iterations:**
  - Remember to add `find_package(GTest CONFIG REQUIRED)` and link `GTest::gtest` and `GTest::gtest_main`.
  - Added `testPresets` to `CMakePresets.json` to allow running `ctest --preset default`.
  - Tests require `enable_testing()` in the top-level CMakeLists.txt.
---

## US-003 (3m22s)

## US-003 — COMPLETED (3m55s)
