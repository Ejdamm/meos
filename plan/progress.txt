## Codebase Patterns
- Use CMake with presets (`CMakePresets.json`) for building.
- Dependencies are managed via `vcpkg` and `vcpkg.json`.
- The modern source code is located in `src/`.
- Executable name is `meos`.
- Redefine common Win32 types (e.g., `SYSTEMTIME`) on non-Windows for legacy compatibility.
- Avoid redundant integer conversion overloads (`itos`, `itow`) that overlap on 64-bit platforms (e.g., `unsigned long` vs `uint64_t`).

## 2026-03-03 23:15 - US-001
- Implemented CMake build system with vcpkg integration.
- Created `vcpkg.json` with `nlohmann-json` and `sqlite3` dependencies.
- Created `CMakeLists.txt` and `CMakePresets.json`.
- Added a minimal `src/main.cpp` that verifies dependencies.
- **Learnings for future iterations:**
  - vcpkg is installed at `/home/adam.georgsson@fnox.it/vcpkg/`.
  - Use `cmake --preset default` to configure and `cmake --build --preset default` to build.
  - The compiler found is GCC 13.3.0.
---

## 2026-03-03 23:30 - US-002
- Created src/ subdirectories for app, domain, net, db, util, io.
- Added CMakeLists.txt to each subdirectory defining static libraries.
- Linked all modules to the main meos executable.
- Verified bare header include works within modules.
- Updated AGENTS.md and created src/AGENTS.md.
- **Learnings for future iterations:**
  - Bare #include within a module works by adding target_include_directories(target PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}).
  - CMake static libraries require at least one source file (e.g., app_dummy.cpp).
---

## 2026-03-03 23:50 - US-003
- Integrated Google Test via vcpkg.
- Configured CMake to enable testing and added a tests/ directory.
- Created tests/CMakeLists.txt and tests/sample_test.cpp.
- Added testPresets to CMakePresets.json.
- Updated root AGENTS.md and created tests/AGENTS.md with testing documentation.
- **Learnings for future iterations:**
  - Remember to add `find_package(GTest CONFIG REQUIRED)` and link `GTest::gtest` and `GTest::gtest_main`.
  - Added `testPresets` to `CMakePresets.json` to allow running `ctest --preset default`.
  - Tests require `enable_testing()` in the top-level CMakeLists.txt.
---

## 2026-03-04 10:45 - US-004
- Created GitHub Actions CI pipeline at .github/workflows/ci.yml.
- Configured workflow to build on Linux (GCC) and Windows (MSVC) in Debug and Release modes.
- Integrated vcpkg in CI for dependency management.
- Added step to run ctest after build.
- Configured build artifacts upload for both platforms.
- **Learnings for future iterations:**
  - Ninja generator is faster and available on ubuntu-latest.
  - vcpkg can be cloned and bootstrapped in CI to ensure the latest version.
  - CMAKE_TOOLCHAIN_FILE must be set to the vcpkg.cmake path in the vcpkg installation directory.
  - Use github.workspace to get the absolute path for toolchain configuration in CI.
---

## US-004 (7m25s)

## 2026-03-04 11:30 - US-005
- Migrated meos_util.h/cpp and meosexception.h to src/util/.
- Refactored meos_util to be cross-platform, using std::chrono, std::filesystem, and standard C++.
- Defined a compatibility SYSTEMTIME struct for Linux.
- Added unit tests in tests/util_test.cpp verifying string conversion and time formatting.
- Created src/util/AGENTS.md with module documentation.
- **Learnings for future iterations:**
  - Redefined SYSTEMTIME on Linux to maintain compatibility with legacy code.
  - Replaced Win32 specific functions (_itow_s, sprintf_s) with standard C++ (to_wstring, snprintf).
  - Used std::filesystem for file existence and move operations.
  - Redundant itos/itow overloads for unsigned long and uint64_t must be avoided on 64-bit Linux where they are identical.
---

## US-005 (18m56s)
