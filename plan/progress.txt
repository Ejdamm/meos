# Ralph Progress Log
Started: sön  8 mar 2026 00:14:34 CET

## Codebase Patterns
- Case Sensitivity: Always use `#include "StdAfx.h"` (not `stdafx.h`).
- Case Sensitivity: File names are generally lowercase (`localizer.h`, `meosexception.h`), but UI components use PascalCase (`TabBase.h`).
- String Utilities: String conversion functions (widen/narrow) are now in `meos_util.h` (US-002).
- Type Aliases: Replaced `DWORD` with `uint32_t` and `BOOL` with `bool` in domain code. (US-004).
- Path Normalization: Use `std::filesystem::path` for all path manipulations and concatenations. (US-005).
---

## sön  8 mar 2026 11:52:10 CET - US-001
- Normalized all #include "..." directives in code/ to match disk casing.
- Files changed: Numerous .h and .cpp files in code/.
- **Learnings for future iterations:**
  - Standardized on StdAfx.h (from stdafx.h).
  - localizer.h, meosexception.h, and TabBase.h had frequent casing mismatches in includes.
  - Using a recursive script that builds a casing database is highly effective for this task.
  - Created a skill for include normalization in .gemini/skills/include-normalization.md.
---

## 2026-03-08 - US-002
- Extracted widen, narrow, toUTF8, fromUTF8, recodeToWide, and recodeToNarrow from gdioutput.h/cpp to meos_util.h/cpp.
- Updated gdioutput.h static methods to be wrappers for the new global functions.
- Batch replaced prefixed calls (gdioutput::widen, gdibase.widen, etc.) with global calls (widen) across the codebase.
- Files changed: meos_util.h/cpp, gdioutput.h/cpp, and numerous domain/utility files.
- **Learnings for future iterations:**
  - String conversion utilities are heavily used across the entire codebase.
  - A common pattern is passing around references to cached strings (StringCache).
  - When batch replacing, be careful with regex as instances (like oe.gdiBase()) might have been partially refactored already.
  - Decoupling domain files from gdioutput.h is limited by other GUI-related calls (addInfoBox, delayAlert) which are still present.
---

## US-002 (18m49s)

## sön  8 mar 2026 12:56:53 CET - US-003
- Replaced Win32-specific string functions (_wtoi, sprintf_s, swprintf_s, _itow_s) with standard C++ equivalents (std::stoi, snprintf, swprintf) in domain files.
- Files changed: Numerous domain files (oEvent*, oRunner, oClass, etc.) and utility files.
- **Learnings for future iterations:**
  - Automated replacement with regex/Python is much faster than manual or agent-driven turns for large files.
  - lang.tl returns const wstring&, which is compatible with swprintf and std::stoi.
  - Always verify buffer types (fixed array vs pointer) before applying sizeof() replacements.
  - Created a skill for string migration in .gemini/skills/string-migration.md.

## US-003 (45m24s)

## sön  8 mar 2026 14:15:22 CET - US-004
- Replaced Win32 types (DWORD, BOOL) with standard C++ types (uint32_t, bool) in domain files.
- Replaced Win32 constants (TRUE, FALSE) with true/false in domain files.
- Added #include <cstdint> where uint32_t is used.
- Files changed: oEvent.h/cpp, oEventSQL.cpp, oEventSpeaker.cpp, oCard.cpp, oClass.cpp, oFreeImport.cpp, MeosSQL.h/cpp, autotask.h/cpp, meos_util.h/cpp, download.h/cpp, onlineinput.h, SportIdent.h/cpp, animationdata.h, random.cpp, image.cpp, meos.cpp, printer.cpp, progress.cpp, testmeos.cpp, mysqldaemon.cpp, xmlparser.cpp.
- **Learnings for future iterations:**
  - Win32 types (DWORD, BOOL) are heavily used in domain logic.
  - Automated replacement with a script is efficient but requires careful filtering to avoid SQL strings (like in MeosSQL.cpp).
  - UI files (Tab*, gdioutput, Table*) still use Win32 types and should be handled separately or left as is if they are to be replaced by a new UI.
  - Created a skill for type migration in .gemini/skills/win32-replacements.md.

## US-004 (20m31s)

## US-004 (11m58s)

## sön  8 mar 2026 13:26:33 CET - US-005
- Replaced hardcoded backslash path separators with std::filesystem::path in domain files.
- Files changed: oClub.cpp, oEvent.cpp, oEventSpeaker.cpp, onlineresults.cpp, meos_util.cpp, meos.cpp, zip.cpp, HTMLWriter.cpp, testmeos.cpp.
- **Learnings for future iterations:**
  - std::filesystem::path is effective for making paths cross-platform.
  - Be careful with parent_path() vs manual trailing slash removal; parent_path() of "dir" is "parent", not "dir".
  - Use (p / "").wstring() to ensure a trailing separator.
  - Libraries and SQL quoting use backslashes for non-path purposes; skip them.
  - Created a skill for path normalization in .gemini/skills/path-normalization.md.
---

## US-005 (17m29s)
