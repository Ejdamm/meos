# Ralph Progress Log
Started: sön  8 mar 2026 00:14:40 CET

## Codebase Patterns
- Use `const object + type` pattern for enums (Vite 7 compatibility)
- Use `import type` for all TypeScript interfaces/types
- Backend: XML API at `/meos`, Frontend: JSON REST API at `/api/v1`
- Use `NavLink` for active route highlighting
- Tailwind 4 for styling (CSS-first approach)
- Use generic `DataTable` component for entity lists with sorting, filtering, and pagination.
- Use `zod` for form validation and `react-hook-form` for form management.
- Reuse standard form components (`FormField`, `FormInput`, \`FormSelect\`, \`SearchableSelect\`) for consistent styling and validation.
- Use `size` property on `FormDialog` ('sm', 'md', 'lg', 'xl') to handle complex forms with varying width requirements.
- `DataTable` supports row selection with `enableSelection` prop.

---
## 2026-03-09 - US-008: Classes page with CRUD
- What was implemented:
  - Created `useCourses.ts` hook for fetching courses.
  - Updated `api/types.ts` to include `StartMethod` enum and `startMethod` field in `Class` interface.
  - Updated `mocks/data.ts` to include `startMethod` in mock classes.
  - Updated `DataTable.tsx` to support `isLoading` prop for better UX.
  - Implemented `ClassesPage.tsx` with full CRUD for classes.
  - Integrated `DataTable`, \`FormDialog\`, \`ConfirmDialog\`, \`FormInput\`, and \`FormSelect\` on the Classes page.
  - Added `zod` validation for class fields (name, courseId, startMethod, sortOrder).
  - Fixed TypeScript issues with `useMutation` and `zodResolver`.
- **Learnings for future iterations:**
  - `useMutation` in the current implementation only takes one argument for `mutate`. Options must be passed to `useMutation` hook itself, or handled via `async/await` in the caller.
  - `z.coerce.number()` might cause type inference issues with `zodResolver` if not careful; sometimes setting `valueAsNumber: true` in `register` and using `z.number()` is cleaner.
  - `FormSelect` values are always strings; remember to convert back to numbers if needed for the API.
  - `DataTable` is a powerful generic component but needs `isLoading` state to avoid flashing "No data" while fetching.
---

## 2026-03-09 10:45:00 - US-009: Courses page with CRUD and control sequence
- What was implemented:
  - Created `useControls.ts` hook for fetching and managing controls.
  - Implemented `ControlSequenceBuilder.tsx` component for managing a course's control sequence.
  - Added `size` property to `FormDialog.tsx` to support different dialog widths ('sm', 'md', 'lg', 'xl').
  - Implemented `CoursesPage.tsx` with full CRUD for courses.
  - Integrated `DataTable`, `FormDialog`, `ConfirmDialog`, `FormInput`, and `ControlSequenceBuilder` on the Courses page.
  - Updated `AGENTS.md` with documentation for the new components and patterns.
- **Learnings for future iterations:**
  - `FormDialog` needed more flexibility for width; added a `size` prop which is useful for complex editors.
  - `ControlSequenceBuilder` uses `SearchableSelect` for adding items, which works well for potentially large lists of controls.
  - Hover states (`opacity-0 group-hover:opacity-100`) provide a clean UI for per-item actions in a list.
---

## US-005 (2m32s)

## iteration-5 (5m29s)

## iteration-6 (2m58s)

## iteration-7 (3m34s)

## iteration-8 (0m26s)

## iteration-9 (1m56s)

## iteration-10 (3m54s)

## iteration-11 (0m09s)

## iteration-12 (1m45s)

## iteration-13 (0m08s)

## iteration-14 (2m57s)

## iteration-15 (3m51s)

## iteration-16 (0m08s)

## iteration-17 (0m07s)

## iteration-18 (0m07s)

## iteration-19 (0m07s)

## iteration-20 (0m08s)

## US-006 (14m52s)

## US-008 (22m11s)

## US-009 (8m54s)

---
## 2026-03-09 11:00:00 - US-010: Controls page with CRUD
- What was implemented:
  - Implemented ControlsPage.tsx with full CRUD for controls.
  - Integrated DataTable, FormDialog, ConfirmDialog, and FormInput on the Controls page.
  - Used zod for form validation (name and code are required).
  - Verified that typecheck passes.
- **Learnings for future iterations:**
  - Followed the established pattern from Classes and Courses pages, which proved efficient.
  - The DataTable and FormDialog components are highly reusable for standard CRUD entities.

## US-010 (5m15s)

---
## 2026-03-09 11:15:00 - US-011: Clubs page with CRUD
- What was implemented:
  - Fixed inconsistency in `useClubs.ts` by updating `useUpdateClub` to use the `{ id, data }` object pattern.
  - Implemented `ClubsPage.tsx` with full CRUD for clubs.
  - Integrated `DataTable`, `FormDialog`, `ConfirmDialog`, and `FormInput` on the Clubs page.
  - Used `zod` for form validation (`name` is required).
  - Verified that typecheck and build pass.
- **Learnings for future iterations:**
  - Standardized the `useUpdateEntity` hook pattern across the codebase improves predictability.
  - Optional string fields in zod should use `.optional().or(z.literal(''))` if the form might return an empty string.

## US-011 (5m35s)

## US-011 (5m23s)

---
## 2026-03-09 11:30:00 - US-012: Runners page with CRUD, search, and filtering
- What was implemented:
  - Updated `useRunners.ts` to use the `{ id, data }` pattern for `useUpdateRunner`, consistent with other entity hooks.
  - Implemented `RunnersPage.tsx` with full CRUD for runners.
  - Integrated `DataTable`, `FormDialog`, `ConfirmDialog`, `FormInput`, `FormSelect`, and `SearchableSelect` on the Runners page.
  - Used `SearchableSelect` for Club and Class assignment in the runner form.
  - Implemented `zod` validation for runner fields.
  - Verified that the Runners page is correctly integrated into the app shell.
  - Confirmed that build and typecheck pass.
- **Learnings for future iterations:**
  - `SearchableSelect` is essential for entities with large numbers of related items (like Clubs and Classes).
  - Standardizing hook patterns (like the `{ id, data }` pattern for updates) prevents implementation errors.
  - Using a grid layout in `FormDialog` (`grid-cols-2`) is better for forms with many fields to avoid excessive vertical scrolling.


## US-012 (6m17s)

---
## 2026-03-09 11:15:00 - US-013: Teams page with CRUD and member management
- What was implemented:
  - Created `useTeams.ts` hook for team CRUD operations.
  - Implemented `TeamMemberManager.tsx` component for managing team members (runners) with reordering support.
  - Implemented `TeamsPage.tsx` with full CRUD for teams, including club and class selection and member management.
  - Updated `AGENTS.md` with documentation for the new components.
  - Verified that build and typecheck pass.
- **Learnings for future iterations:**
  - `FormDialog` and `ConfirmDialog` use `open` instead of `isOpen`, and `FormDialog` uses `onSave` instead of `onSubmit`. This inconsistency was fixed in the implementation.
  - `handleSubmit` from `react-hook-form` returns a function that expects an optional event; when used in a custom `onSave` handler, it must be called explicitly as `() => handleSubmit(onSubmit)()`.
  - Used `as any` for `zodResolver` in this specific case to bypass complex type inference issues between Zod's coerced types and the expected form values, which is a pragmatic approach for rapid migration but should be investigated for a cleaner fix in the future.


## US-013 (11m15s)

## iteration-8 (60m00s)

---
## 2026-03-09 12:00:00 - US-014: Runner import from CSV and IOF XML
- What was implemented:
  - Created `ImportRunnersDialog.tsx` component for multi-step runner import.
  - Implemented client-side CSV parsing using `papaparse`.
  - Integrated server-side IOF XML import via `useImportXMLRunners` hook.
  - Added a preview step using `DataTable` to review imported runners before final submission.
  - Implemented bulk creation using `useBulkCreateRunners` hook.
  - Added an "Import" button to `RunnersPage.tsx`.
  - Updated `AGENTS.md` with documentation for the new dialog.
  - Verified that build and typecheck pass.
- **Learnings for future iterations:**
  - `DataTable` does not use a `pagination` object prop; it expects `pageSize` directly.
  - CSV header mapping should be flexible (e.g., supporting "Name", "name", "Full Name").
  - Using a multi-step dialog (Upload -> Preview -> Import) provides a much better UX than immediate import.
  - MSW handlers for bulk operations and file uploads were essential for testing this feature without a real backend.

## US-014 (3m11s)

---
## 2026-03-09 13:30:00 - US-015: Bulk operations on runners
- What was implemented:
  - Created `Checkbox.tsx` UI component.
  - Updated `DataTable.tsx` to support row selection with checkboxes and select-all functionality.
  - Added `updateRunnersBulk` and `assignStartTimesBulk` to the API client.
  - Added MSW handlers for bulk runner updates and start time assignment.
  - Implemented bulk action toolbar on `RunnersPage.tsx`.
  - Added dialogs for bulk class assignment and bulk status change.
  - Added confirmation dialog for bulk start time assignment.
  - Verified that build and typecheck pass.
- **Learnings for future iterations:**
  - `DataTable` needs a stable `getItemId` prop (defaulting to `id`) to track selection correctly across filtering and sorting.
  - Using a dedicated selection state (`selectedItems`) in `DataTable` makes it easy to implement bulk actions in the parent page.
  - Bulk actions should always have a confirmation step or a clear visual indicator of the affected items.

---
## 2026-03-09 15:30:00 - US-017: Live results with auto-refresh
- What was implemented:
  - Updated `useResults.ts` to support silent refresh (without setting `isLoading` state).
  - Added "Live" mode toggle to `ResultsPage.tsx`.
  - Implemented configurable polling interval (5s, 10s, 20s, 30s, 60s).
  - Added row change detection and highlight animation (yellow pulse) for 5 seconds after a change.
  - Added visual indicators for live mode status and last refresh time.
  - Verified that build and typecheck pass.
- **Learnings for future iterations:**
  - Silent refresh is crucial for live updates to avoid flickering the entire UI.
  - Tracking previous state with `Map` and `useEffect` is an effective way to detect changes in a list.
  - Using CSS transitions (`transition-colors duration-1000`) makes the highlight fade-out smooth.

---
## 2026-03-09 16:00:00 - US-018: Results and start list export
- What was implemented:
  - Added `exportResultsXML` and `exportStartListXML` to the API client.
  - Created `useCompetition.ts` hook for competition metadata.
  - Added CSV and IOF XML export buttons to `ResultsPage.tsx` and `StartListPage.tsx`.
  - Implemented client-side CSV generation using `papaparse`.
  - Implemented file download triggering with dynamic filenames (including competition name and date).
  - Verified that build and typecheck pass.
- **Learnings for future iterations:**
  - `Papa.unparse` is a reliable way to generate CSV from objects.
  - Blobs and temporary `<a>` elements are the standard way to trigger client-side downloads.
  - Reusing the API client for export URLs keeps the endpoint logic centralized.

## US-016 — COMPLETED (9m26s)
