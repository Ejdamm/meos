# Ralph Progress Log
Started: sön  8 mar 2026 00:14:40 CET

## Codebase Patterns
- Use `const object + type` pattern for enums (Vite 7 compatibility)
- Use `import type` for all TypeScript interfaces/types
- Backend: XML API at `/meos`, Frontend: JSON REST API at `/api/v1`
- Use `NavLink` for active route highlighting
- Tailwind 4 for styling (CSS-first approach)
- Use generic `DataTable` component for entity lists with sorting, filtering, and pagination.
- Use `zod` for form validation and `react-hook-form` for form management.
- Reuse standard form components (`FormField`, `FormInput`, `FormSelect`, `SearchableSelect`) for consistent styling and validation.

---
## 2026-03-09 - US-006 & US-007: Form Components and Competition Page
- What was implemented:
  - Verified and finalized reusable form components: `FormField`, `FormInput`, `FormSelect`, `SearchableSelect`.
  - Finalized `FormDialog` and `ConfirmDialog` components.
  - Implemented `CompetitionPage.tsx` with a full CRUD form for competition settings.
  - Added `zod` validation for competition fields (name, date, organizer, homepage, zeroTime).
  - Updated MSW handlers to support single competition fetching and updating by ID.
  - Integrated `useQuery` and `useMutation` hooks for competition data management.
- **Learnings for future iterations:**
  - `react-hook-form` works seamlessly with `forwardRef` components like `FormInput`.
  - MSW handlers should be kept in sync with the `api/client.ts` to avoid "mismatched" endpoint assumptions.
  - Simple `alert` for feedback is sufficient for early iterations, but a dedicated `Toast` component should be considered for better UX.
  - Always check for existing components before implementing new ones, as some might have been partially completed in previous runs or iterations.
---

## US-005 (2m32s)

## iteration-5 (5m29s)

## iteration-6 (2m58s)

## iteration-7 (3m34s)

## iteration-8 (0m26s)

## iteration-9 (1m56s)

## iteration-10 (3m54s)

## iteration-11 (0m09s)

## iteration-12 (1m45s)

## iteration-13 (0m08s)

## iteration-14 (2m57s)

## iteration-15 (3m51s)

## iteration-16 (0m08s)

## iteration-17 (0m07s)

## iteration-18 (0m07s)

## iteration-19 (0m07s)

## iteration-20 (0m08s)

## US-006 (14m52s)
