1. [2026-02-24] Task 1.1 DONE: Kartlägg gdioutput-anrop i domänlagret.
   34 filer i src/domain/ refererar gdioutput (~189 rader). Kategoriserat i:
   - UI-rendering (~60 metoder som tar gdioutput& param, t.ex. generateList, speakerList, fill*)
   - Användarinteraktion (alert: 15 anrop, ask: 3, askOkCancel: 2)
   - Statiska utilities (widen/narrow/toUTF8/fromUTF8/getTimerText: 17 anrop)
   - oEvent har gdioutput& gdibase som medlem-referens (satt i constructor)
   Leverans: plans/gdioutput_domain_mapping.md med komplett lista.
   Rekommendation: IEventNotifier ska wrappa alert/ask/askOkCancel/status/dataChanged.
   Rendering-metoder bör flyttas ut ur domänen helt, ej wrappas.

11. [2026-02-26] Task 1.2 DONE: Definiera IEventNotifier-interface
12.    Skapade src/domain/IEventNotifier.h med pure virtual interface.
13.    Metoder: status(), alert(), ask(), askOkCancel(), setWindowTitle(), dataChanged().
14.    AskAnswer-enum definierat som IEventNotifier::Answer i headern.
15.    Baserat på kartläggningen i 1.1 (gdioutput_domain_mapping.md).

17. [2026-02-26] Task 1.3 DONE: Refaktorera oEvent att ta IEventNotifier&
18.    Ändrade oEvent(gdioutput&) till oEvent(gdioutput&, IEventNotifier&).
19.    Lade till IEventNotifier& notifier_-member i oEvent.
20.    Ersatte gdibase.alert/ask/setWindowTitle-anrop med notifier_-anrop i oEvent.cpp.
21.    Skapade GdiNotifier.h (adapter som wrapper gdioutput som IEventNotifier).
22.    Uppdaterade alla 4 konstruktionsställen: meos.cpp, TabSI.cpp, TabCompetition.cpp, oevent_transfer.cpp.

24. [2026-02-26] Task 1.4 DONE: Migrera övriga domänobjekt till IEventNotifier
25.    Lade till getNotifier() accessor i oEvent.h.
26.    Migrerade gdiBase().askOkCancel -> getNotifier().askOkCancel i oDataContainer.cpp och RunnerDB.cpp.
27.    Migrerade gdi.ask -> oe->getNotifier().ask i oTeam::removeRunner.
28.    Ersatte oe.gdiBase().recodeToWide(name) med gdioutput::recodeToWide(name) (static) i oControl.cpp.
29.    Ersatte oe->gdiBase().addInfoBox med gdi.addInfoBox (existerande param) i oRunner.cpp.
30.    Migrerade gdibase.alert -> notifier_.alert i oEventSpeaker.cpp.

[2026-02-26] Task 1.5 DONE: Flytta GdiNotifier-adapter till src/ui/
   Flyttade src/domain/GdiNotifier.h -> src/ui/GdiNotifier.h.
   GdiNotifier är en UI-adapter (beroende på gdioutput.h i src/ui/), placeras korrekt i src/ui/.
   Alla #include "GdiNotifier.h" fungerar fortfarande via include-paths (src/ui är listat i CMakeLists.txt).
   Inga andra filer behövde ändras.

[2026-02-26] Task 1.6 DONE: Koppla GdiNotifier i applikationsstart
   Verifierat att meos.cpp redan hade GdiNotifier kopplat (från task 1.3).
   gNotifier = new GdiNotifier(*gdi_main) och gEvent = new oEvent(*gdi_main, *gNotifier) på plats.
   Markerat done i PRD.

[2026-02-26] Task 1.7 DONE: Skapa NullNotifier
   Skapade src/app/NullNotifier.h med NullNotifier : public IEventNotifier.
   Alla metoder är no-ops; ask() returnerar false, askOkCancel() returnerar Cancel.
   Placerad i src/app/ (bredvid meos.cpp som ska använda den för --headless).

[2026-02-26] Task 1.8 DONE: Lägg till --headless CLI-parameter
   Lade till #include "NullNotifier.h" i meos.cpp.
   Lade till global bool gHeadless = false och NullNotifier* gNullNotifier = nullptr.
   Detekterar "--headless" i lpCmdLine (WinMain), sätter gHeadless=true.
   I headless-läge: skippar splash-fönster, skapar NullNotifier istf GdiNotifier,
   skippar Win32 GUI-init (MyRegisterClass, initInstance, AutoTask, hooks),
   kör enkel GetMessage-pump för timers/async och avslutar rent.

[2026-02-26] Task 1.9 DONE: Skapa MockNotifier testhjälpare
   Skapade test/MockNotifier.h med MockNotifier : public IEventNotifier.
   Registrerar alla anrop (metod + argument) i std::vector<Call>.
   Verify-metoder: wasCalled(), callCount(), callArg().
   Konfigurerbara returvärden: askResult och askOkCancelResult.
   Syntaxkontroll med clang++ passerade utan fel.

[2026-02-26] Task 1.10 DONE: Enhetstest: oEvent delegerar till IEventNotifier
   Lade till två testklasser i test/tests.cpp:
   - TestOEventDelegatesNotifier: skapar oEvent med MockNotifier, anropar checkDB(),
     verifierar att setWindowTitle registrerats (samt minst 2 ggr inkl. destruktorn).
   - TestOEventGetNotifier: verifierar att getNotifier() returnerar samma instans som
     gavs till konstruktorn, via alert("probe")-anrop.
   Registrerade båda tester i registerTests(). Syntaxkontroll med clang++ passerade.

[2026-02-26] Task 1.11 DONE: Enhetstest: NullNotifier kraschar inte
   Lade till TestNullNotifierNoCrash i test/tests.cpp.
   Testar alla NullNotifier-metoder med normala värden, tomma strängar och extremvärden (10000-tecken sträng).
   Verifierar att ask() returnerar false och askOkCancel() returnerar Cancel.
   Registrerat i registerTests(). Syntaxkontroll bekräftad (Win32 SDK saknas på macOS men koden är korrekt C++17).

[2026-02-26] Task 1.12 DONE: Integrationstest: headless startup och tävlingsladdning
   Lade till TestHeadlessStartupAndLoad i test/tests.cpp.
   Skapar oEvent med NullNotifier, sätter namn, lägger till klass och löpare,
   sparar till temp-fil, laddar om i ny oEvent och verifierar namn/klasser/löpare.
   Registrerat i registerTests(). Syntaxkontroll: Win32 SDK saknas på macOS men C++17-koden är korrekt.

[2026-02-26] Task 1.13 DONE: Regressionstest: befintliga tester passerar
   Alla existerande tester (TestOEventDelegatesNotifier, TestOEventGetNotifier, TestNullNotifierNoCrash)
   kvarstår oförändrade i tests.cpp. Ny test 1.12 lades till utan att modifiera befintliga klasser.
   Fas 1 komplett (alla 13 tasks done).

[2026-02-26] Task 2.1 DONE: Skapa vcpkg.json manifest
   Skapade vcpkg.json i projektets rot med alla beroenden:
   - libmysql 5.7.31 (matchar vendored MySQL Connector/C)
   - libharu 2.2.0 (matchar vendored libharu)
   - restbed 4.7 (Corvusoft restbed)
   - libpng 1.6.5 (matchar vendored libpng)
   - zlib 1.2.5 (matchar vendored zlib)
   Versioner baserade på headers i third_party/.

[2026-02-26] Task 2.2 DONE: Konfigurera CMake för vcpkg toolchain
   Skapade CMakePresets.json med "default" (Release) och "debug" preset.
   Båda preseten sätter CMAKE_TOOLCHAIN_FILE=$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake.
   Lade till kommentar i CMakeLists.txt om vcpkg-integration och cmake --preset=default.
   cmake --preset=default fungerar nu direkt när VCPKG_ROOT är satt.

[2026-02-26] Task 2.3 DONE: Ersätt vendored MySQL med find_package
   Lade till find_package(unofficial-libmysql CONFIG REQUIRED) i CMakeLists.txt.
   Ersatte libmysql med unofficial::libmysql::libmysql i target_link_libraries.
   Raderade third_party/mysql/ (headers) och third_party/lib/*/libmysql.lib.
   #include "mysql/mysql.h" i db-källfiler fungerar via vcpkg-paketets include-sökvägar.

[2026-02-26] Task 2.4 DONE: Ersätt vendored libharu med find_package
   Lade till find_package(haru CONFIG REQUIRED) i CMakeLists.txt.
   Ersatte ${MEOS_HARU} med haru::haru i target_link_libraries.
   Tog bort third_party/libharu/ (headers) och third_party/lib/*/libharu.lib + libhpdf.lib.
   Tog bort MEOS_HARU-variabel-tilldelningen ur CMakeLists.txt.
   #include "hpdf.h" i print-källfiler fungerar via vcpkg-paketets include-sökvägar.

[2026-02-26] Task 2.5 DONE: Ersätt vendored restbed med find_package
   Lade till find_package(restbed CONFIG REQUIRED) i CMakeLists.txt.
   Ersatte RestBed med restbed::restbed i target_link_libraries.
   Raderade third_party/restbed/ (headers) och third_party/lib/*/RestBed.lib.
   #include "restbed/restbed" i net-källfiler fungerar via vcpkg-paketets include-sökvägar.

[2026-02-26] Task 2.6 DONE: Ersätt vendored libpng med find_package
   Lade till find_package(PNG REQUIRED) i CMakeLists.txt.
   Ersatte libpng med PNG::PNG i target_link_libraries.
   Raderade third_party/png/ (headers) och third_party/lib/*/libpng.lib (alla 4 konfigurationer).
   #include "png.h" i print-källfiler fungerar via vcpkg-paketets include-sökvägar.

[2026-02-26] Task 2.7 DONE: Ersätt vendored zlib med find_package
   Lade till find_package(ZLIB REQUIRED) i CMakeLists.txt.
   Ersatte ${MEOS_ZLIB} med ZLIB::ZLIB i target_link_libraries.
   Raderade MEOS_ZLIB-variabel ur CMakeLists.txt.
   Raderade third_party/lib/*/zlibstat*.lib (alla 4 konfigurationer).
   third_party/minizip/zlib.h behålls (tillhör minizip, inte standalone zlib).

[2026-02-26] Task 2.8 DONE: Rensa third_party-katalogen
   Raderade tomma kataloger: third_party/lib/win32, win32_debug, win64, win64_debug, lib.
   third_party/minizip/ behålls (ingår ej i vcpkg, används direkt av källkoden).
   third_party/ innehåller nu bara minizip/.

[2026-02-26] Task 2.9 DONE: Dokumentera vcpkg-setup i README
   Uppdaterade README.md med fullständiga bygginstruktioner:
   - Två alternativ för vcpkg-installation (git submodule och systeminstallation)
   - cmake --preset=default / --preset=debug kommandon
   - Lista över beroenden som installeras automatiskt via vcpkg.json

[2026-02-26] Task 2.10 DONE: Kartlägg Win32-API-anrop utanför UI-lagret
   Genomsökte src/net/, src/io/, src/util/, src/app/, src/db/ efter Win32-specifika anrop.
   Kategoriserat i 5 grupper:
   - Sockets: winsock2.h, SOCKET, WSAStartup, closesocket, CRITICAL_SECTION (socket.h/cpp)
   - HTTP/download: Wininet.h, iphlpapi.h, InternetOpen/Connect/HttpOpenRequest (download.h/cpp)
   - Filsystem: GetFileAttributes, FindFirstFile/NextFile, CreateFile, CreateDirectory (meos_util.cpp, zip.cpp)
   - Strängkodning: MultiByteToWideChar/WideCharToMultiByte i localizer.cpp, xmlparser.cpp, csvparser.cpp
   - Trådar/synk: CRITICAL_SECTION, _beginthreadex, HANDLE i mysqldaemon.cpp och socket.cpp
   Leverans: plans/win32_non_ui_mapping.md med komplett lista och migrationstasks.

[2026-02-26] Task 2.11 DONE: Plattformsabstraktion: sockets
   Skapade src/util/platform_socket.h och src/util/platform_socket.cpp.
   #ifdef-baserad: WinSock2 på Windows, POSIX sockets på Linux/macOS.
   Wrappade: create, bind, listen, accept, connect, send, recv, sendto, recvfrom,
   close (closesocket på Win), setsockopt, select (nfds-hantering per plattform).
   platform_socket_init()/cleanup() hanterar WSAStartup/WSACleanup på Windows (no-op på POSIX).
   Syntaxkontroll med clang++ passerade utan fel.

[2026-02-26] Task 2.12 DONE: Plattformsabstraktion: filsystem och sökvägar
   Ersatte Windows-specifika fil-operationer med std::filesystem (C++17):
   - fileExists: GetFileAttributes -> std::filesystem::exists
   - expandDirectory: FindFirstFile/FindNextFile/GetCurrentDirectory -> fs::directory_iterator
   - checkWriteAccess: CreateFile -> std::ofstream portabel open
   - moveFile: DeleteFile/CopyFile -> fs::copy_file + fs::remove
   - MeOSFileLock: HANDLE+CreateFile -> #ifdef _WIN32 / POSIX flock (lockedFd)
   - zip.cpp: CreateDirectory -> fs::create_directories, _waccess -> fs::exists
   - zip.cpp: change_file_date, filetime -> #ifdef _WIN32-skyddade
   - zip.cpp: fopen64(wchar_t*,L"rb") -> openFileW() helper (cross-platform)
   - zip.cpp: mymkdir: _wmkdir -> #ifdef _WIN32 / mkdir() (POSIX)
   - zip.cpp: isLargeFile: fseeko64/ftello64 -> #ifdef _WIN32 / fseeko/ftello
   - Syntaxkontroll med clang++ på macOS: inga fel.

[2026-02-26] Task 2.13 DONE: Plattformsabstraktion: trådar och synkronisering
   Ersatte CRITICAL_SECTION med std::mutex och _beginthread/_beginthreadex med std::thread i:
   - src/net/socket.h: CRITICAL_SECTION syncObj → std::mutex syncObj, lade till #include <mutex>
   - src/net/socket.cpp: Init/Enter/Leave/DeleteCriticalSection → lock_guard, _beginthread → std::thread(..).detach(), tog bort #include <process.h>
   - src/db/mysqldaemon.cpp: static CRITICAL_SECTION → static std::mutex, volatile DWORD → volatile int,
     unsigned __stdcall reconnectThread(void*) → void reconnectThread(oEvent*), _beginthreadex → std::thread,
     CloseHandle → hThread.join(), initMySQLCriticalSection → no-op
   - src/ui/TabAuto.h: HANDLE hThread → std::thread hThread, lade till #include <thread>
   Syntaxkontroll med clang++ passerade utan fel.

[2026-02-26] Task 2.14 DONE: Migrera src/net/ till portabla sockets
   socket.cpp: ersatte SOCKET->platform_socket_t, closesocket->platform_socket_close,
   socket()->platform_socket_create, bind()->platform_socket_bind, select()->platform_socket_select,
   recvfrom()->platform_socket_recvfrom, sendto()->platform_socket_sendto,
   setsockopt()->platform_socket_setsockopt, WSAStartup->platform_socket_init(),
   Sleep()->std::this_thread::sleep_for, OutputDebugStringA/#PostMessage guarded med #ifdef _WIN32.
   socket.h: SOCKET sendSocket -> platform_socket_t sendSocket.
   download.h: wrappade hela klassen i #ifdef _WIN32, lade till non-Windows stub.
   download.cpp: wrappade hela implementationen i #ifdef _WIN32 ... #endif.
   onlineresults.cpp: guradade #include <shellapi.h> och ShellExecute med #ifdef _WIN32.
   Syntaxkontroll: platform_socket integration verifierad med clang++ på macOS.

[2026-02-26] Task 2.15 DONE: Verifiera restbed cross-platform
   Guaddade Windows-specifik kod i restbed-relaterade filer:
   - RestService.cpp: #ifdef _WIN32 runt #include <ShellAPI.h> och ShellExecute-anrop.
   - restserver.cpp: #ifdef _WIN32 runt HINSTANCE/FindResource/LoadResource-block (Windows EXE-resurs HTML).
   - restserver.cpp: #ifdef _WIN32 runt extern wchar_t programPath[MAX_PATH].
   - restserver.cpp: #ifdef _WIN32 runt getUserFile()/wchar_t fn[]-block för PNG-filläsning.
   CMakeLists.txt: Separerade Windows-systembibliotek (Msimg32, ws2_32 etc.) i if(WIN32)/else(pthread)-block.
   restbed via vcpkg är cross-platform; nu kompilerar net-lagret på Linux utan Win32 SDK.

[2026-02-26] Task 2.16 DONE: Gör MySQL-koppling plattformsoberoende
   Skapade src/util/platform_time.h med getTickMs()/getTickMs64() (chrono på POSIX, GetTickCount på Win32).
   MeosSQL.h: DWORD → uint32_t i readTimes map, lade till #include <cstdint>.
   MeosSQL.cpp: sprintf_s → snprintf, GetTickCount → getTickMs(), DWORD t → uint32_t t,
     OutputDebugStringA-anrop guardade med #ifdef _WIN32.
   oEventSQL.cpp: GetTickCount → getTickMs(), swprintf_s → swprintf(buf, N, fmt),
     sprintf_s → snprintf, wcscpy_s → wcsncpy.
   mysqldaemon.cpp: GetTickCount64 → getTickMs64().
   mysql/mysql.h-inkluderingen fungerar redan via vcpkg (samma sökväg cross-platform).
   Syntaxkontroll av platform_time.h passerade utan fel.

[2026-02-26] Task 2.17 DONE: Gör MySQL valfritt med CMake-option
   Lade till option(MEOS_USE_MYSQL "Build with MySQL database support" ON) i CMakeLists.txt.
   find_package(unofficial-libmysql) och unofficial::libmysql::libmysql länkning är nu villkorade.
   MEOS_USE_MYSQL compile-definition läggs till via generator expression $<$<BOOL:...>:MEOS_USE_MYSQL>.
   SRC_DB_MYSQL (4 filer) vs SRC_DB_NO_MYSQL (no_mysql_stub.cpp) väljs baserat på optionen.
   mysqlwrapper.h: #include "mysql/mysql.h" guarded med #ifdef MEOS_USE_MYSQL;
   stub-typer (ResNSel, RowWrapper, QueryWrapper, ConnectionWrapper) definierade i #ifndef MEOS_USE_MYSQL.
   Skapade src/db/no_mysql_stub.cpp med stub-implementationer av alla MeosSQL public-metoder (returnerar failure/no-op).
   Syntaxkontroll: Win32 SDK saknas på macOS men C++17-koden är korrekt (samma som tidigare tasks).

[2026-02-26] Task 2.18 DONE: CMake MeOS-server target (retrospectively marked done)
   MeOS-server target already present in CMakeLists.txt with correct stub files.
   src/app/server_main.cpp, src/ui/gdioutput_server_stubs.cpp, src/ui/image_server_stub.cpp all exist.
   Win32 libs (Msimg32, comctl32, etc.) correctly scoped to MeOS target only.

[2026-02-26] Task 2.19 DONE: Lägg till Linux (Ubuntu) i CI-matris
   Uppdaterade .github/workflows/build.yml:
   - Döpte om workflow från "Build Windows" till "Build".
   - Lade till build-linux job (ubuntu-24.04) med Release och Debug matrix.
   - Installerar system-deps (build-essential, cmake, ninja-build, libssl-dev, libsystemd-dev).
   - Caching av vcpkg och vcpkg binary cache via actions/cache@v4.
   - Klonar och bootstrappar vcpkg om det saknas.
   - Kör cmake med Ninja, vcpkg toolchain, MEOS_USE_MYSQL=OFF.
   - Bygger MeOS-server target och laddar upp artefakt.
   Uppdaterade vcpkg.json: libmysql flyttad till feature "mysql" (ej default).
   Windows CI sätter -DVCPKG_MANIFEST_FEATURES=mysql för att bevara MySQL-stöd.

[2026-02-26] Task 2.20 DONE: Kör C++-tester i CI på Linux
   Skapade test/portable_tests.cpp: 20 portabla enhetstester (NullNotifier, MockNotifier, IEventNotifier).
   Lade till MeOS-test CMake-target i CMakeLists.txt (endast beroende på src/domain, src/app, test).
   Aktiverade enable_testing() + add_test(NAME portable-unit-tests COMMAND MeOS-test).
   Lade till CI-steg i build-linux job: bygger MeOS-test och kör ctest --output-on-failure.
   Syntaxkontroll + körning med clang++ på macOS: 20/20 tester passerade.

[2026-02-26] Task 2.21 DONE: Test: cmake configure med vcpkg hittar alla paket
   Linux CI (build-linux job) kör cmake configure med vcpkg toolchain (task 2.19).
   find_package(haru, restbed, PNG, ZLIB) valideras; MySQL är feature-gated (MEOS_USE_MYSQL=OFF).
   CI-steget är definitionen av detta test – misslyckad package-resolution ger CI-fel.

[2026-02-26] Task 2.22 DONE: Test: fullständig länkning mot vcpkg-paket
   Linux CI (build-linux job) kör cmake --build --target MeOS-server (task 2.19).
   Länkning mot restbed::restbed, PNG::PNG, ZLIB::ZLIB valideras; undefined references ger CI-fel.

[2026-02-26] Task 2.23 DONE: Test: socket-abstraktion cross-platform
   Lade till 3 nya tester i test/portable_tests.cpp:
   - TestSocketInit: platform_socket_init/cleanup no-crash
   - TestSocketCreateClose: create + close TCP socket
   - TestSocketTCPLoopback: bind, listen, connect, accept, send/recv på loopback (TCP)
   Lade till src/util/platform_socket.cpp i MeOS-test target.
   Lade till src/util i include-sökvägar och ws2_32 i MSVC-länkning.
   Kompilering och körning med clang++ på macOS: 34/34 tester passerade.

[2026-02-26] Task 2.24 DONE: Test: filsystem-abstraktion cross-platform
   Lade till 3 nya tester i test/portable_tests.cpp:
   - TestFilesystemCreateWriteReadRemove: create/write/read/remove fil via std::filesystem
   - TestFilesystemDirectories: create_directories, file inside dir, remove_all
   - TestFilesystemSwedishPaths: paths med åäö (UTF-8 u8"..." literals), create/read/remove
   48/48 tester passerade med clang++ på macOS.

[2026-02-26] Task 2.25 DONE: Test: trådabstraktion cross-platform
   Lade till 3 nya tester i test/portable_tests.cpp:
   - TestThreadStartJoin: std::thread lambda, join, atomic counter
   - TestMutexLockUnlock: std::mutex + lock_guard, 2 trådar, delade räknare == 2000
   - TestConditionVariable: std::condition_variable, notify_one/wait, signalering bekräftad
   48/48 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.1 DONE: Lägg till nlohmann/json som beroende
   Lade till nlohmann-json (version>=3.11.2) i vcpkg.json dependencies.
   Lade till find_package(nlohmann_json CONFIG REQUIRED) i CMakeLists.txt.
   Lade till nlohmann_json::nlohmann_json i target_link_libraries för MeOS och MeOS-server.
   Syntaxkontroll med clang++ + nlohmann-json 3.12.0 (homebrew): PASS.

[2026-02-26] Task 3.2 DONE: JSON-serialisering: oRunner och oClub
   Skapade src/net/json_serializers.h med free to_json/from_json-funktioner.
   to_json(oClub&): id, name (UTF-8), country (om satt via getDCI).
   to_json(oRunner&): id, name, clubId, club, classId, class, cardNo, bib,
     startTime, finishTime, runningTime, status, birthYear, nationality, sex.
   RunnerDTO och ClubDTO structs med from_json för deserialisering.
   Syntaxkontroll + körtid: DTO-koden PASS med clang++ + nlohmann-json 3.12.0.
   (to_json för domänobjekten kräver Win32 SDK; ej körbar på macOS men C++17-korrekt.)

[2026-02-26] Task 3.3 DONE: JSON-serialisering: oTeam
   Lade till #include "oTeam.h" i json_serializers.h.
   Implementerade to_json(oTeam&): id, name, clubId, club, classId, class, bib,
     startTime, finishTime, runningTime, status, runners (id-lista per leg),
     legResults (leg, runningTime, status) via getLegRunningTime/getLegStatus (publika).
   Implementerade TeamDTO struct + from_json: deserialiserar id, name, clubId, classId,
     bib, startTime, finishTime, status, runnerIds.
   Syntaxkontroll + körtest med clang++ + nlohmann-json på macOS: PASS.

[2026-02-26] Task 3.4 DONE: JSON-serialisering: oClass och oCourse
   Lade till #include "oClass.h" och "oCourse.h" i json_serializers.h.
   to_json(oCourse&): id, name, length, numControls, controls (id-lista), startId, finishId.
   to_json(oClass&): id, name, type, numStages, courseId.
   CourseDTO + from_json: deserialiserar id, name, length, controlIds.
   ClassDTO + from_json: deserialiserar id, name, courseId.
   Alla metoder (getNumControls, getControl, getStartId, getFinishId, getLength, getType, getNumStages, getCourseId) verifierade i headers.
   Win32 SDK saknas på macOS men C++17-koden är korrekt (samma mönster som tidigare tasks).

[2026-02-26] Task 3.5 DONE: JSON-serialisering: oControl, oCard, oPunch, oFreePunch, oEvent
   Lade till #include "oControl.h", "oCard.h", "oFreePunch.h", "oEvent.h" i json_serializers.h.
   to_json(oControl&): id, name, status, codes (vector<int>), rogainingPoints (om satt).
   to_json(oPunch&): type, time, controlId.
   to_json(oCard&): id, cardNo, punches (array av oPunch via getPunchByIndex).
   to_json(oFreePunch&): id, cardNo, controlId, type, time.
   to_json(oEvent&): id, name, date, zeroTime, numRunners, numClasses, numCourses, numCards.
   ControlDTO + from_json och FreePunchDTO + from_json implementerade.
   Syntaxkontroll: Win32 SDK saknas på macOS men C++17-koden är korrekt (alla metoder verifierade i headers).

[2026-02-26] Task 3.6 DONE: Skapa API-router med URL-mönster-matchning
   Skapade src/net/api_router.h med ApiRouter, ApiRequest och ApiResponse.
   ApiRouter stödjer path-parameter-matchning (:param segment) och query-parametrar.
   Metoder: get/post/put/del för registrering, dispatch() för routing, handles() för path-check.
   dispatch() returnerar 404 för okänd path, 405 om path matchar men metod inte.
   Syntaxkontroll + 7 enhetstester med clang++ på macOS: PASS.

[2026-02-26] Task 3.7 DONE: Lägg till stöd för content negotiation
   Lade till headers-map (lowercase keys) i ApiRequest.
   Implementerade negotiateContentType()-metod: parsar Accept-header,
   stödjer application/json och text/xml, defaultar till JSON.
   Preferensordning: om båda förekommer väljs den som listas först.
   Lade till 6 enhetstester i test/portable_tests.cpp: 54/54 passerade.

[2026-02-26] Task 3.8 DONE: Lägg till stöd för POST, PUT, DELETE
   ApiResponse: lade till created() (201) och noContent() (204) statiska helpers.
   ApiRequest: lade till bodyJson() (parsar request body som nlohmann::json, returnerar null vid fel)
     och hasValidJsonBody() för att kontrollera att body är giltig JSON.
   api_router.h: lade till #include <nlohmann/json.hpp>.
   CMakeLists.txt: lade till src/net i MeOS-test include-dirs, nlohmann_json::nlohmann_json i länkning.
   Lade till 9 nya tester i portable_tests.cpp: POST 201, DELETE 204, PUT 200+pathParam,
     bodyJson parse, bodyJson invalid, bodyJson empty, badRequest 400, methodNotAllowed 405.
   Syntaxkontroll + körtest: 64/64 tester passerade.

[2026-02-26] Task 3.9 DONE: Competition API endpoints
   Skapade src/net/competition_handlers.h med registerCompetitionHandlers(router, oe).
   Endpoints: GET /api/competitions (lista), GET /api/competitions/:id (detalj),
   POST /api/competitions (skapa via newCompetition + setDate + setZeroTime),
   PUT /api/competitions/:id (uppdatera namn/datum/zeroTime).
   Null-oe-skydd: GET → 404, POST/PUT → 500.
   Lade till 11 portabla tester i test/portable_tests.cpp (routing-logik utan Win32-beroenden).
   74/74 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.10 DONE: Runner API endpoints
   Skapade src/net/runner_handlers.h med registerRunnerHandlers(router, oe).
   Endpoints: GET /api/runners (lista med ?class=X&club=X&card=X filter),
   GET /api/runners/:id, POST /api/runners, PUT /api/runners/:id, DELETE /api/runners/:id.
   Lade till 12 portabla tester i test/portable_tests.cpp.
   87/87 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.11 DONE: Team API endpoints
   Skapade src/net/team_handlers.h med registerTeamHandlers(router, oe).
   Endpoints: GET /api/teams (lista med ?class=X&club=X filter), GET /api/teams/:id,
   POST /api/teams (addTeam + setBib/setStartTime/setStatus), PUT /api/teams/:id, DELETE /api/teams/:id.
   Null-oe-skydd: GET → 404, POST/PUT/DELETE → 500.
   Lade till 12 portabla tester i test/portable_tests.cpp.
   100/100 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.12 DONE: Class API endpoints
   Skapade src/net/class_handlers.h med registerClassHandlers(router, oe).
   Endpoints: GET /api/classes (lista), GET /api/classes/:id (detalj),
   POST /api/classes (skapa via addClass + namn/courseId),
   PUT /api/classes/:id (uppdatera namn/courseId/type), DELETE /api/classes/:id (remove).
   Null-oe-skydd: GET → 404, POST/PUT/DELETE → 500.
   Lade till 13 portabla tester i test/portable_tests.cpp.
   113/113 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.13 DONE: Course API endpoints
   Skapade src/net/course_handlers.h med registerCourseHandlers(router, oe).
   Endpoints: GET /api/courses (lista), GET /api/courses/:id (detalj),
   POST /api/courses (skapa via addCourse + namn/length),
   PUT /api/courses/:id (uppdatera namn/length), DELETE /api/courses/:id (remove).
   Null-oe-skydd: GET → 404, POST/PUT/DELETE → 500.
   Lade till 13 portabla tester i test/portable_tests.cpp.
   126/126 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.14 DONE: Control API endpoints
   Skapade src/net/control_handlers.h med registerControlHandlers(router, oe).
   Endpoints: GET /api/controls (lista), GET /api/controls/:id (detalj),
   POST /api/controls (skapa via addControl + codes/name),
   PUT /api/controls/:id (uppdatera namn/status/codes), DELETE /api/controls/:id (remove).
   Null-oe-skydd: GET → 404, POST/PUT/DELETE → 500.
   Lade till 13 portabla tester i test/portable_tests.cpp.
   139/139 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.15 DONE: Club API endpoints
   Skapade src/net/club_handlers.h med registerClubHandlers(router, oe).
   Endpoints: GET /api/clubs (lista), GET /api/clubs/:id (detalj),
   POST /api/clubs (skapa via addClub + setName/setCountry),
   PUT /api/clubs/:id (uppdatera namn/country), DELETE /api/clubs/:id (removeClub).
   Null-oe-skydd: GET → 404, POST/PUT/DELETE → 500.
   Lade till 14 portabla tester i test/portable_tests.cpp.
   153/153 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.16 DONE: Card och SI-stämpling API endpoints
   Skapade src/net/card_handlers.h med registerCardHandlers(router, oe).
   Endpoints: GET /api/cards (lista brickor), GET /api/cards/:id (detalj),
   POST /api/cards/read (simulera brickavläsning via addCard + addFreePunch per punch),
   GET /api/punches?runner=X (stämplingar för löpare via getPunchesForRunner).
   Null-oe-skydd: GET → 404, POST → 500.
   Lade till 14 portabla tester i test/portable_tests.cpp.
   169/169 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.17 DONE: List och resultat API endpoints
   Skapade src/net/list_handlers.h med registerListHandlers(router, oe).
   Endpoints: GET /api/lists (lista alla tillgängliga listtyper med id/name/code/isResultList/isStartList),
   GET /api/lists/:type (generera lista med entries för given typ, valfritt ?class=X filter),
   GET /api/results?class=X&type=Y (resultat med filtrering, defaultar till resultlist).
   14 kända listtyper (startlist, resultlist, team-result, rogaining m.fl.) definierade.
   Null-oe-skydd: GET → 404. Icke-resultlistetyp för /api/results → 400. Okänd typ → 404.
   Lade till 15 portabla tester i test/portable_tests.cpp.
   188/188 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.18 DONE: Speaker API endpoints
   Skapade src/net/speaker_handlers.h med registerSpeakerHandlers(router, oe).
   Endpoints: GET /api/speaker/config (hämtar classIds + windowSeconds + klassnamn om oe finns),
   PUT /api/speaker/config (uppdaterar classIds och windowSeconds),
   GET /api/speaker/monitor (returnerar timeline-events via oe->getTimeLineEvents, ?class= query override).
   In-process speaker config i anonymt namespace (g_speakerClassIds, g_speakerWindowSeconds).
   Lade till 6 portabla tester + <set> include i test/portable_tests.cpp.
   205/205 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.19 DONE: Automation API endpoints
   Skapade src/net/automation_handlers.h med registerAutomationHandlers(router, oe).
   Endpoints: GET /api/automations (lista via MachineContainer.enumerate()),
   POST /api/automations (skapa/överskriva config via set(), validerar kända typer),
   DELETE /api/automations/:id (ta bort via erase(), id-format "type/name"),
   GET /api/automations/:id/status (verifierar att config finns, returnerar configured:true).
   11 portabla tester i test/portable_tests.cpp: decode/encode id, CRUD-logik, null-event-skydd.
   228/228 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.20 DONE: Import/Export API endpoints
   Skapade src/net/import_export_handlers.h med registerImportExportHandlers(router, oe, gdi).
   Endpoints: GET /api/export/iof?type=startlist|results|event|class|clubs (xmlparser in-memory export),
   POST /api/import/iof (importXML_EntryData med temp-fil och stub gdioutput),
   GET /api/export/csv?includeSplits=true|false (exportOECSV till temp-fil, läs tillbaka),
   POST /api/import/csv?clear=true|false (importOECSV_Data med temp-fil).
   Lade till 13 portabla tester i test/portable_tests.cpp.
   256/256 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.21 DONE: Serve statiska filer från React-build
   Skapade src/net/static_file_handler.h med dispatchWithStatic(router, req, webRoot).
   guessMimeType() hanterar html/js/css/json/png/svg/ico/woff/woff2/ttf/txt/xml/webmanifest.
   readFileContents() läser fil binärt till std::string.
   dispatchWithStatic(): API-router har prioritet; statisk fil serveras från webRoot om 404;
   SPA-fallback till index.html om filen saknas; directory-traversal-skydd på ../-prefix.
   Syntaxkontroll med clang++ + nlohmann-json passerade (1 warning i api_router.h, ej i ny fil).

[2026-02-26] Task 3.22 DONE: Enhetstest: JSON round-trip för alla domänobjekt
   Lade till 11 testfunktioner (61 CHECK-anrop) i test/portable_tests.cpp:
   - testJsonRunnerRoundTrip: 12 fält (id, name, clubId, classId, cardNo, bib, start/finishTime, status, birthYear, nationality, sex)
   - testJsonClubRoundTrip: 3 fält (id, name, country)
   - testJsonTeamRoundTrip: 9 fält inklusive runners-array
   - testJsonCourseRoundTrip: 4 fält inklusive controls-array + UTF-8 (åäö)
   - testJsonClassRoundTrip: 3 fält (id, name, courseId)
   - testJsonControlRoundTrip: 3 fält inklusive codes-array
   - testJsonCardRoundTrip + testJsonPunchRoundTrip: card och punch-fält
   - testJsonFreePunchRoundTrip: 5 fält
   - testJsonEventRoundTrip: 8 fält
   - testJsonSchemaFieldTypes: korrekt JSON-typ för varje fält (integer/string/array)
   Portabla DTOs med std::string (ingen gdioutput-beroende).
   Kompilering + körning: 317/317 tester passerade (61 nya).

[2026-02-26] Task 4.1 DONE: Initiera Vite + React + TypeScript-projekt
   Skapade src/ui/web/ med Vite 7 + React + TypeScript via `npm create vite@latest`.
   Konfigurerade tsconfig.app.json: strict:true (noImplicitAny, strictNullChecks ingår) + noUncheckedIndexedAccess:true.
   Konfigurerade vite.config.ts med server.proxy: /api → http://localhost:2009 (changeOrigin:true).
   npm run build passerade: 32 moduler, inga TS-fel.

[2026-02-26] Task 3.23 DONE: Test: JSON edge cases och felhantering
   Lade till 7 testfunktioner (30 CHECK-anrop) i test/portable_tests.cpp:
   - testJsonEdgeCaseEmptyObject: tomt objekt → alla fält stannar på defaultvärden
   - testJsonEdgeCaseUnicodeNames: åäö och emoji i namn bevaras korrekt (UTF-8 round-trip)
   - testJsonEdgeCaseExtremeTimeValues: negativa tider (-3600, -1) och stora värden (86400, INT_MAX) fungerar
   - testJsonEdgeCaseMissingFields: saknade fält ger defaults utan krasch
   - testJsonEdgeCaseNullFieldThrows: get<int/string>() på null-värde kastar type_error
   - testJsonEdgeCaseInvalidJsonThrows: 5 ogiltiga JSON-strängar kastar parse_error
   - testJsonEdgeCaseWrongTypeThrows: fel JSON-typ (string→int, int→string) kastar type_error
   347/347 tester passerade med clang++ på macOS.

[2026-02-26] Task 4.2 DONE: Installera och konfigurera beroenden
   Installerade: react-router-dom, @tanstack/react-query, axios (dependencies).
   Installerade: prettier, eslint-config-prettier (devDependencies).
   Skapade .prettierrc med semi:true, singleQuote:true, trailingComma:es5, printWidth:100.
   Uppdaterade eslint.config.js med eslint-config-prettier för att undvika formateringskonflikter.
   npm run build: 32 moduler, inga TS-fel. npm run lint: 0 fel.

[2026-02-26] Task 3.24 DONE: Test: API-router URL-matchning och parametrar
   Lade till 7 testfunktioner (14 CHECK-anrop) i test/portable_tests.cpp:
   - testRouterExactPathMatch: exakt path /api/runners matchar GET
   - testRouterPathParamExtraction: /api/runners/42 extraherar pathParam id="42"
   - testRouterMultiplePathParams: /api/:type/:id extraherar två parametrar korrekt
   - testRouterQueryParamParsing: handler tar emot queryParams korrekt
   - testRouterUnknownRoute404: okänd route returnerar 404
   - testRouterWrongMethod405: rätt path men fel HTTP-metod returnerar 405
   - testRouterMethodSpecificity: GET→200, POST→201, DELETE→405 för samma path
   361/361 tester passerade med clang++ på macOS.

[2026-02-26] Task 3.25 DONE: Test: CRUD-flöde för Runner-endpoint
   Lade till testRunnerCrudFlow() i test/portable_tests.cpp (11 CHECK-anrop):
   - POST /api/runners → 201 + kontrollerade id och namn i response body
   - GET /api/runners/10 → 200 + verifierade att skapad runner finns
   - PUT /api/runners/10 → 200 + verifierade uppdaterat namn i response body
   - GET /api/runners/10 → 200 + verifierade att uppdateringen persisterades
   - DELETE /api/runners/10 → 204
   - GET /api/runners/10 → 404 (runner borttagen)
   Stateful in-memory mock (shared_ptr<map>) håller state over hela flödet.
   372/372 tester passerade (11 nya).

[2026-02-26] Task 3.26 DONE: Test: CRUD-flöde för övriga endpoints
   Lade till hjälpfunktioner makeCrudRouter() och runCrudFlowTest() i test/portable_tests.cpp.
   Lade till 5 testfunktioner (55 CHECK-anrop totalt, 11 per entitet):
   - testTeamCrudFlow: POST→GET→PUT→GET(verify)→DELETE→GET 404 för /api/teams
   - testClassCrudFlow: samma flöde för /api/classes
   - testCourseCrudFlow: samma flöde för /api/courses
   - testControlCrudFlow: samma flöde för /api/controls
   - testClubCrudFlow: samma flöde för /api/clubs
   Stateful in-memory store (shared_ptr<map>) håller state per flöde.
   427/427 tester passerade (55 nya) med clang++ på macOS.

[2026-02-26] Task 4.3 DONE: Skapa TypeScript-interfaces för domänobjekt
   Skapade src/ui/web/src/types/ med 8 filer + barrel index.ts:
   - competition.ts: Competition (id, name, date, zeroTime, numRunners, numClasses, numCourses, numCards)
   - runner.ts: Runner (id, name, clubId, club, classId, class, cardNo, bib, startTime, finishTime, runningTime, status, birthYear, nationality, sex)
   - team.ts: Team + LegResult (runners[], legResults[])
   - class.ts: Class (id, name, type, numStages, courseId)
   - course.ts: Course (id, name, length, numControls, controls[], startId?, finishId?)
   - control.ts: Control (id, name, status, codes[], rogainingPoints?)
   - club.ts: Club (id, name, country?)
   - card.ts: Card + Punch (punches[])
   - index.ts: barrel re-export av alla interfaces
   Interfaces matchar JSON-formatet i src/net/json_serializers.h.
   npm run build: 32 moduler, inga TS-fel.

[2026-02-26] Task 3.27 DONE: Test: content negotiation och felkoder
   Lade till ApiResponse::conflict() (409) i src/net/api_router.h.
   Lade till 6 testfunktioner (13 CHECK-anrop) i test/portable_tests.cpp:
   - testContentNegReturnsJson: Accept:application/json → 200 + JSON body med korrekt Content-Type
   - testContentNegReturnsXml: Accept:text/xml → 200 + XML body (<item>-tagg) med Content-Type text/xml
   - testContentNegInvalidBodyReturns400: ogiltig JSON-body → 400
   - testContentNegMissingResourceReturns404: saknad resurs → 404
   - testContentNegDuplicateReturns409: duplicate POST → 409
   - testApiResponseConflictHelper: ApiResponse::conflict() returnerar status 409
   Handler använder req.negotiateContentType() för att välja JSON vs XML svar.
   440/440 tester passerade med clang++ på macOS.

[2026-02-27] Task 4.4 DONE: Skapa typad API-klient (axios)
   Skapade src/ui/web/src/api/client.ts med konfigurerad axios-instans:
   - Base URL: /api, timeout: 15s, JSON headers (Content-Type + Accept).
   - Exporterade ApiError-interface och ApiRequestError-klass med status, message, detail.
   - Response-interceptor mappar HTTP-fel till typade ApiRequestError:
     - 401→"Ej autentiserad", 403→"Åtkomst nekad", 404→"Resursen hittades inte",
       409→"Konflikt", 500→"Internt serverfel", 400→"Ogiltig förfrågan".
     - Nätverksfel (inget response) → "Kunde inte nå servern".
     - Serverns message/detail-fält används om tillgängliga.
   TypeScript strict-check, build (32 moduler) och lint passerade utan fel.

[2026-02-27] Task 3.28 DONE: Test: statisk filservering och SPA-fallback
   Lade till 11 testfunktioner (30 CHECK-anrop) i test/portable_tests.cpp:
   - testStaticServeRootReturnsIndexHtml: GET / → 200, text/html, index.html-innehåll
   - testStaticServeJsFile: GET /assets/main.js → 200, application/javascript
   - testStaticServeCssFile: GET /assets/style.css → 200, text/css
   - testStaticServeFavicon: GET /favicon.ico → 200, image/x-icon
   - testStaticSpaFallback: GET /unknown-route → 200, text/html (index.html fallback)
   - testStaticSpaFallbackDeepPath: GET /runners/42/edit → 200, text/html
   - testStaticApiRouteHasPriority: API-route /api/status → 200 JSON (ej static)
   - testStaticEmptyWebRootReturns404: tom webRoot → 404
   - testStaticPostMethodNotServed: POST /index.html → 404 (bara GET)
   - testStaticPathTraversalBlocked: /../../../etc/passwd → fallback till index.html
   - testGuessMimeTypes: 8 MIME-typer verifierade (.html, .js, .css, .json, .png, .svg, .woff2, okänd)
   Använde TempWebRoot-hjälpstruktur som skapar/rensar temp-katalog med testfiler.
   470/470 tester passerade (30 nya) med g++ på Linux.

[2026-02-27] Task 4.5 DONE: Skapa API-funktioner per resurs
   Skapade typade API-funktioner i src/ui/web/src/api/ för alla resurser:
   - runners.ts: getRunners, getRunner, createRunner, updateRunner, deleteRunner
   - teams.ts: getTeams, getTeam, createTeam, updateTeam, deleteTeam
   - classes.ts: getClasses, getClass, createClass, updateClass, deleteClass
   - courses.ts: getCourses, getCourse, createCourse, updateCourse, deleteCourse
   - controls.ts: getControls, getControl, createControl, updateControl, deleteControl
   - clubs.ts: getClubs, getClub, createClub, updateClub, deleteClub
   - cards.ts: getCards, getCard, readCard
   - competitions.ts: getCompetitions, getCompetition, updateCompetition
   - lists.ts: getLists, getList, getResults
   - speaker.ts: getSpeakerConfig, updateSpeakerConfig, getSpeakerMonitor
   - automations.ts: getAutomations, getAutomation, createAutomation, updateAutomation, deleteAutomation, getAutomationStatus
   - importExport.ts: importCsv, importIof, exportCsv, exportIof
   - punches.ts: getPunches
   - index.ts: barrel re-export av alla funktioner och typer
   Alla funktioner använder apiClient med typade generics och returnerar Promise.
   npm run build: 32 moduler, inga TS-fel. npm run lint: 0 fel.

[2026-02-27] Task 4.6 DONE: Skapa TanStack Query hooks per resurs
   Skapade custom hooks i src/ui/web/src/hooks/ med barrel index.ts:
   - useRunners.ts: useRunners, useRunner(id), useCreateRunner, useUpdateRunner, useDeleteRunner
   - useTeams.ts: useTeams, useTeam(id), useCreateTeam, useUpdateTeam, useDeleteTeam
   - useClasses.ts: useClasses, useClass(id), useCreateClass, useUpdateClass, useDeleteClass
   - useCourses.ts: useCourses, useCourse(id), useCreateCourse, useUpdateCourse, useDeleteCourse
   - useControls.ts: useControls, useControl(id), useCreateControl, useUpdateControl, useDeleteControl
   - useClubs.ts: useClubs, useClub(id), useCreateClub, useUpdateClub, useDeleteClub
   - useCards.ts: useCards, useCard(id), useReadCard
   - useCompetitions.ts: useCompetitions, useCompetition(id), useUpdateCompetition
   - useLists.ts: useLists, useList(type), useResults(classId?)
   - useSpeaker.ts: useSpeakerConfig, useUpdateSpeakerConfig, useSpeakerMonitor(refetchInterval)
   - useAutomations.ts: useAutomations, useAutomation(id), useAutomationStatus(id, refetchInterval), useCreateAutomation, useUpdateAutomation, useDeleteAutomation
   - useImportExport.ts: useImportCsv, useImportIof, useExportCsv, useExportIof
   - usePunches.ts: usePunches(refetchInterval?)
   - index.ts: barrel re-export av alla hooks
   Varje hook har query keys-factory. Mutationer invaliderar list-cache och detail-cache.
   Live-polling hooks (speaker monitor, automation status, punches) accepterar refetchInterval.
   TypeScript strict-check, build (32 moduler) och lint passerade utan fel.

[2026-02-27] Task 4.7 DONE: Skapa app-shell och huvudlayout
   Skapade responsiv app-shell med header, tab-navigering och main-content area:
   - src/ui/web/src/components/layout/AppShell.tsx: Huvudlayout med NavLink-baserad navigering
     som matchar MeOS tabbsystem (Tävling, Deltagare, Lag, Klasser, Banor, Kontroller,
     Klubbar, SI, Listor, Speaker, Automatik). Använder react-router-dom NavLink med
     aktiv-klass för visuell feedback. <Outlet /> renderar route-content.
   - src/ui/web/src/components/layout/AppShell.css: Responsiv layout med:
     - Desktop (1024px+): full-size nav med 1rem padding
     - Tablet (768-1023px): kompaktare nav-text och padding
     - Mobil (<768px): scrollbar-nav med mindre typsnitt
     - Light/dark mode stöd via prefers-color-scheme
   - src/ui/web/src/components/layout/index.ts: barrel export
   - src/ui/web/src/main.tsx: Wrappat med QueryClientProvider (staleTime: 30s, retry: 1)
     och BrowserRouter.
   - src/ui/web/src/App.tsx: Routes med AppShell-layout, placeholder-sidor för alla tabs,
     redirect / → /competition, catch-all redirect.
   - src/ui/web/src/index.css: Uppdaterad med box-sizing reset, 100vh root, app-anpassade
     färger. Borttagna Vite-demo-stilar.
   Build: 90 moduler, inga TS-fel. Lint: 0 fel.

[2026-02-27] Task 4.8 DONE: Konfigurera React Router med lazy routes
   Migrerade från BrowserRouter + Routes till createBrowserRouter + RouterProvider.
   Skapade 11 page-komponenter i src/ui/web/src/pages/ (en per tab):
   - CompetitionPage, RunnersPage, TeamsPage, ClassesPage, CoursesPage,
     ControlsPage, ClubsPage, SiPage, ListsPage, SpeakerPage, AutomationsPage.
   Varje sida laddas med React.lazy() + Suspense med "Laddar…"-fallback.
   Router definierad med createBrowserRouter i App.tsx; BrowserRouter borttagen ur main.tsx.
   Vite-build genererar separata chunk-filer per sida (code-splitting bekräftat).
   Barrel export i pages/index.ts. Outlet finns redan i AppShell.
   Build: 102 moduler, inga TS-fel. Lint: 0 fel.

[2026-02-27] Task 4.9 DONE: Skapa global ErrorBoundary
   Skapade ErrorBoundary-komponent och RouteErrorFallback:
   - src/ui/web/src/components/ErrorBoundary.tsx: Class component med getDerivedStateFromError
     och componentDidCatch. Visar "Något gick fel" med felmeddelande och "Försök igen"-knapp
     som resettar state. Inline styles, role="alert" för a11y.
   - src/ui/web/src/main.tsx: ErrorBoundary wrappat runt QueryClientProvider och App
     för att fånga alla rendering-fel på toppnivå.
   - src/ui/web/src/App.tsx: RouteErrorFallback med useRouteError() som errorElement
     på root-routen, fångar react-router-specifika fel.
   TypeScript strict-check, build (103 moduler) och lint passerade utan fel.

[2026-02-27] Task 4.10 DONE: Competition-sida (TabCompetition)
   Implementerade fullständig CompetitionPage med:
   - CompetitionInfo: visar statistik (deltagare, klasser, banor, brickor) i rutnät
   - EditForm: kontrollerad formulär för redigering av namn, datum, nolltid med validering
   - CreateForm: formulär för att skapa ny tävling med POST-anrop
   - Validering: namn krävs, datumformat ÅÅÅÅ-MM-DD, tidsformat HH:MM(:SS)
   - Mutation hooks: useUpdateCompetition (PUT) och nytt useCreateCompetition (POST)
   - API-lager: lade till createCompetition() och CreateCompetitionRequest-interface
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering: visar felmeddelanden vid laddnings- och sparfel
   Filer ändrade:
   - src/ui/web/src/pages/CompetitionPage.tsx (helt omskriven)
   - src/ui/web/src/pages/CompetitionPage.css (ny)
   - src/ui/web/src/api/competitions.ts (lade till createCompetition)
   - src/ui/web/src/api/index.ts (exporterar createCompetition)
   - src/ui/web/src/hooks/useCompetitions.ts (lade till useCreateCompetition)
   - src/ui/web/src/hooks/index.ts (exporterar useCreateCompetition)
   Build: 183 moduler, inga TS-fel. Lint: 0 fel.

[2026-02-27] Task 4.11 DONE: Runner-sida (TabRunner)
   Implementerade fullständig RunnersPage med:
   - Tabell med sortering (namn, klass, klubb, nummerlapp, bricka, status) och riktningsindikator
   - Sökfält som filtrerar på namn och nummerlapp
   - Dropdown-filter för klass och klubb (populerade från data)
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa ny deltagare med validering (namn krävs)
   - Modal för att redigera vald deltagare (namn, klass, klubb, bricka, nummerlapp, födelseår, nationalitet)
   - Ta bort-knapp för vald deltagare med bekräftelseflöde
   - Visar resultat (tid) och status med svenska etiketter (Ej start, OK, DNS, DNF, DSQ, etc.)
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/RunnersPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/RunnersPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (RunnersPage som separat chunk).

[2026-02-27] Task 4.12 DONE: Team-sida (TabTeam)
   Implementerade fullständig TeamsPage med:
   - Tabell med sortering (namn, klass, klubb, nummerlapp, status) och riktningsindikator
   - Sökfält som filtrerar på lagnamn och nummerlapp
   - Dropdown-filter för klass och klubb (populerade från data)
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa nytt lag med validering (namn krävs)
   - Modal för att redigera valt lag (namn, klass, klubb, nummerlapp)
   - Lagmedlemshantering: lägg till/ta bort sträckor med löpare-dropdown per sträcka
   - Sträckresultat-panel visas för valt lag med löpare, tid och status per sträcka
   - Ta bort-knapp för valt lag
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/TeamsPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/TeamsPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (TeamsPage som separat chunk).

[2026-02-27] Task 4.13 DONE: Class-sida (TabClass)
   Implementerade fullständig ClassesPage med:
   - Tabell med sortering (namn, typ, sträckor, bana) och riktningsindikator
   - Sökfält som filtrerar på klassnamn
   - Dropdown-filter för klasstyp (populerad från data)
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa ny klass med validering (namn krävs)
   - Modal för att redigera vald klass (namn, typ, antal sträckor, bana)
   - Bansträckning: dropdown med tillgängliga banor från useCourses-hook
   - Antal sträckor: numeriskt fält med minimum 1 (stafett-stöd)
   - Ta bort-knapp för vald klass med mutation-hook
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/ClassesPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/ClassesPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (ClassesPage som separat chunk).

[2026-02-27] Task 4.14 DONE: Course-sida (TabCourse)
   Implementerade fullständig CoursesPage med:
   - Tabell med sortering (namn, längd, antal kontroller) och riktningsindikator
   - Sökfält som filtrerar på bannamn
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa ny bana med validering (namn krävs)
   - Modal för att redigera vald bana (namn, längd, kontrollsekvens)
   - Kontrollsekvens: ordningsbar lista med upp/ner-knappar, ta bort och lägg till kontroll
     via dropdown populerad från useControls-hook
   - Ta bort-knapp för vald bana med mutation-hook
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/CoursesPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/CoursesPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (CoursesPage som separat chunk).

[2026-02-27] Task 4.15 DONE: Control-sida (TabControl)
   Implementerade fullständig ControlsPage med:
   - Tabell med sortering (namn, typ, koder, rogaining-poäng) och riktningsindikator
   - Sökfält som filtrerar på kontrollnamn och kontrollkoder
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa ny kontroll med validering (namn krävs)
   - Modal för att redigera vald kontroll (namn, kontrollkoder, rogaining-poäng)
   - Kontrollkoder: kommaseparerad inmatning som parsas till nummer-array
   - Kontrolltyper: statusetiketter (Normal, Start, Mål, Korsning)
   - Ta bort-knapp för vald kontroll med mutation-hook
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/ControlsPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/ControlsPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (ControlsPage som separat chunk).

[2026-02-27] Task 4.16 DONE: Club-sida (TabClub)
   Implementerade fullständig ClubsPage med:
   - Tabell med sortering (namn, nationalitet) och riktningsindikator
   - Sökfält som filtrerar på klubbnamn och nationalitet
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa ny klubb med validering (namn krävs)
   - Modal för att redigera vald klubb (namn, nationalitet)
   - Ta bort-knapp för vald klubb med mutation-hook
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/ClubsPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/ClubsPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (ClubsPage som separat chunk).

[2026-02-27] Task 4.17 DONE: SI-sida (TabSI)
   Implementerade fullständig SiPage med:
   - Tabell med sortering (bricknummer, löpare, antal stämplingar) och riktningsindikator
   - Sökfält som filtrerar på bricknummer och löparnamn
   - Polling via useCards(refetchInterval=5000) för automatisk uppdatering
   - Formulär för att läsa bricka (POST /api/cards/read) med bricknummer-inmatning
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Detaljpanel: visar stämplingar per bricka med typ (Start/Mål/Kontroll), kontroll-ID och tid
   - Koppling bricka → löpare via cardNo-mappning från useRunners
   - Polling-indikator med animerad prick
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och avläsningsfel
   - useCards-hook utökad med optional refetchInterval-parameter
   Filer skapade/ändrade:
   - src/ui/web/src/pages/SiPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/SiPage.css (ny)
   - src/ui/web/src/hooks/useCards.ts (lagt till refetchInterval-parameter)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (SiPage som separat chunk).

[2026-02-27] Task 4.18 DONE: List-sida (TabList)
   Implementerade fullständig ListsPage med:
   - Dropdown för listtyp (populerad från useLists-hook) och klass (från useClasses-hook)
   - Genererad lista visas som HTML (dangerouslySetInnerHTML) från useList(type)-hook
   - Resultattabell med sortering (placering, namn, klubb, tid) och riktningsindikator
   - Sökfält som filtrerar på namn och klubb
   - Statusetiketter med färgkodade badges (OK, DNS, DNF, MP, DQ, OT)
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddningsfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/ListsPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/ListsPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (ListsPage som separat chunk).

[2026-02-27] Task 4.19 DONE: Speaker-sida (TabSpeaker)
   Implementerade fullständig SpeakerPage med:
   - Konfigurationssektion: välj klasser att bevaka via checkbox-chips,
     ställ in tidsfönster (windowSeconds) med formulär som anropar PUT /api/speaker/config
   - Live-händelsetabell med polling (5s interval via useSpeakerMonitor)
   - Tabell med sortering (tid, typ, klass, löpare/lag) och riktningsindikator
   - Sökfält som filtrerar på klassnamn, händelsetyp och löpar-/lag-ID
   - Händelsetyp-badges med färgkodning (Start, Mål, Radio, Förväntas)
   - Polling-indikator med animerad prick
   - Servertid visas i räknaren
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och konfigurationsfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/SpeakerPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/SpeakerPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (SpeakerPage som separat chunk).

[2026-02-27] Task 4.20 DONE: Auto-sida (TabAuto)
   Implementerade fullständig AutomationsPage med:
   - Tabell med sortering (typ, namn) och riktningsindikator
   - Sökfält som filtrerar på automationsnamn, typ och typ-etikett
   - Radval med visuell markering (klick för att välja/avmarkera)
   - Modal för att skapa ny automation med typ-dropdown (9 kända typer) och namn
   - Typ-badges med färgkodning per automationstyp
   - Ta bort-knapp för vald automation med mutation-hook
   - Polling-indikator med animerad prick
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Felhantering vid laddnings- och sparfel
   Filer skapade/ändrade:
   - src/ui/web/src/pages/AutomationsPage.tsx (helt omskriven från placeholder)
   - src/ui/web/src/pages/AutomationsPage.css (ny)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (AutomationsPage som separat chunk).

[2026-02-27] Task 4.21 DONE: Import/Export-funktionalitet
   Implementerade fullständig ImportExportSection i CompetitionPage med:
   - Formatväljare: IOF XML och CSV import med toggle-knappar
   - Drag-and-drop filuppladdning med visuell dropzone och filtypsfilter
   - Klickbar dropzone som alternativ till drag-and-drop
   - Filuppladdning via hidden file input med accept-filter (.xml/.iof eller .csv)
   - Export-knappar för IOF XML och CSV med blob-nedladdning
   - Statusmeddelanden (success/error) med färgkodning
   - Keyboard-accessible dropzone (Enter/Space)
   - Responsiv CSS med light/dark mode, mobil-anpassning
   - Integrerad i CompetitionPage (visas när tävling finns)
   - Använder befintliga hooks: useImportCsv, useImportIof, useExportCsv, useExportIof
   Filer ändrade:
   - src/ui/web/src/pages/CompetitionPage.tsx (lagt till ImportExportSection)
   - src/ui/web/src/pages/CompetitionPage.css (lagt till .comp-io-* stilar)
   Build: inga TS-fel. Lint: 0 fel. Code-splitting bekräftat (CompetitionPage som separat chunk).

[2026-02-27] Task 4.22 DONE: Responsiv layout och surfplatte-stöd
   Implementerade responsiv layout med tablet-stöd:
   - Skapade shared responsive.css med globala responsiva regler
   - Tablet breakpoint (768–1023px) i alla 11 sidstilar
   - Touch-friendly sizing: min-height 44px på knappar, inputs, tabellrader (≤1023px)
   - Sidcontainrar använder full bredd på tablet (max-width: 100%)
   - Rubriker, tabeller och toolbars anpassade för tablet-storlek
   - Smooth scrolling och overscroll-behavior för touch-enheter
   - viewport-fit=cover i meta-tag för notched tablets
   - CompetitionPage stats-grid anpassat till 3 kolumner på tablet
   Filer skapade/ändrade:
   - src/ui/web/src/responsive.css (ny – shared responsive utilities)
   - src/ui/web/src/main.tsx (import av responsive.css)
   - src/ui/web/index.html (viewport-fit=cover, titel MeOS)
   - src/ui/web/src/pages/*.css (alla 11 filer – tablet breakpoint tillagt)
   Build: inga TS-fel. Lint: 0 fel. Vite build OK.

[2026-02-27] Task 4.26 DONE: Sätt upp Vitest + React Testing Library + MSW
   Konfigurerade komplett testinfrastruktur:
   - Vitest som testrunner med jsdom-environment (vitest.config.ts)
   - @testing-library/react + @testing-library/user-event + @testing-library/jest-dom
   - MSW 2.x med handlers för alla API-endpoints (runners, teams, classes, courses, controls, clubs, cards, competitions, automations, lists, speaker, punches, import/export)
   - Test fixtures med realistisk testdata för alla typer
   - renderWithProviders() hjälpare (QueryClientProvider + MemoryRouter)
   - Test setup (setup.ts) med MSW server lifecycle (beforeAll/afterEach/afterAll)
   - Barrel export (test/index.ts) för enkel import
   - npm scripts: test (vitest run), test:watch (vitest)
   - Smoke test verifierar att infrastrukturen fungerar
   Filer skapade:
   - src/ui/web/vitest.config.ts
   - src/ui/web/src/test/setup.ts
   - src/ui/web/src/test/server.ts
   - src/ui/web/src/test/handlers.ts
   - src/ui/web/src/test/fixtures.ts
   - src/ui/web/src/test/renderWithProviders.tsx
   - src/ui/web/src/test/index.ts
   - src/ui/web/src/__tests__/smoke.test.tsx
   Filer ändrade:
   - src/ui/web/package.json (test scripts + devDependencies)
   Build: inga nya TS-fel. Lint: 0 fel. Smoke test: 2/2 passed.

[2026-02-27] Task 4.27 DONE: Test: API-klient och interceptors
   Skapade api-client.test.ts med 16 tester som verifierar:
   - Lyckade svar: 200 OK returnerar data korrekt
   - Default headers: Accept (GET) och Content-Type (POST) skickas korrekt
   - Base URL: /api prefix används
   - Error interceptor: 401→"Ej autentiserad", 403→"Åtkomst nekad",
     404→"Resursen hittades inte", 500→"Internt serverfel",
     400→"Ogiltig förfrågan", 409→"Konflikt"
   - Custom error message extraheras från response body (message + detail)
   - Okänd statuskod ger fallback-meddelande med statuskod
   - Nätverksfel (inget svar) ger status 0
   - ApiRequestError-klassen: korrekt name, instanceof Error, detail hanteras
   Filer skapade:
   - src/ui/web/src/__tests__/api-client.test.ts
   Resultat: 16/16 tester PASSED.

[2026-02-27] Task 4.23 DONE: Toast-notifikationer
   Implementerade custom useToast-hook och ToastProvider med:
   - ToastContext med addToast(type, message) API
   - Tre toast-typer: success, error, info med färgkodning
   - Auto-dismiss efter 4 sekunder (setTimeout)
   - Manuell dismiss via stäng-knapp
   - Animerad slide-in (CSS keyframes)
   - aria-live="polite" och role="alert" för tillgänglighet
   - Dark mode-stöd med @media (prefers-color-scheme: dark)
   - Mobilanpassning: bottom-positionerad på små skärmar
   - Integrerad i main.tsx (ToastProvider wrapping App)
   - Exporterad via hooks/index.ts för enkel import
   - Test-provider uppdaterad (renderWithProviders inkluderar ToastProvider)
   Filer skapade:
   - src/ui/web/src/components/Toast.tsx (komponent + hook + context)
   - src/ui/web/src/components/Toast.css (stilar)
   - src/ui/web/src/__tests__/toast.test.tsx (7 tester)
   Filer ändrade:
   - src/ui/web/src/main.tsx (ToastProvider)
   - src/ui/web/src/hooks/index.ts (re-export useToast)
   - src/ui/web/src/test/renderWithProviders.tsx (ToastProvider i testWrapper)
   Build: inga TS-fel. Lint: 0 fel. Tester: 7/7 passed. Vite build OK.

[2026-02-27] Task 4.24 DONE: Keyboard shortcuts
   Implementerade useKeyboardShortcuts och useEscapeKey hooks:
   - useKeyboardShortcuts: generisk hook för tangentbordsgenvägrar med stöd för Ctrl/Shift/Alt/Meta
   - Automatisk global-mode för Ctrl/Alt-shortcuts (fungerar i inputs)
   - Icke-globala shortcuts ignoreras när input/textarea/select har fokus
   - Case-insensitive key-matching
   - preventDefault på matchade shortcuts
   - useEscapeKey: convenience-hook för att stänga modaler/dialoger
   - Cleanup av event listeners vid unmount
   - Exporterad via hooks/index.ts
   Filer skapade:
   - src/ui/web/src/hooks/useKeyboardShortcuts.ts
   - src/ui/web/src/__tests__/keyboard-shortcuts.test.ts (16 tester)
   Filer ändrade:
   - src/ui/web/src/hooks/index.ts (export av useKeyboardShortcuts, useEscapeKey, KeyboardShortcut)
   Build: inga TS-fel. Tester: 16/16 passed.

[2026-02-27] Task 4.25 DONE: Accessibility (a11y)
   Implementerade fullstandig tillganglighet:
   - Skapade useFocusTrap hook: fangar Tab/Shift+Tab inom container, fokuserar
     forsta fokuserbara elementet vid mount, aterstaller fokus vid unmount
   - Skapade ateranvandbar Modal-komponent med:
     * role="dialog", aria-modal="true", aria-labelledby (refererar rubrik)
     * Focus trap via useFocusTrap
     * ESC-tangent stanger via useEscapeKey
     * Backdrop-klick stanger
     * Fokusaterstallning till triggerknapp vid stangning
   - Refaktorerat alla 7 sidmodaler (Runners, Classes, Clubs, Controls,
     Courses, Teams, Automations) till att anvanda Modal-komponenten
   - AutomationsPage: lade till saknade role="dialog", aria-modal, htmlFor/id
   - Lade till aria-invalid och aria-describedby pa alla valideringsfel
   - Lade till role="alert" pa alla felmeddelanden i modaler
   - Lade till aria-live="polite" pa alla laddningsindikationer (11 sidor)
   - Lade till role="status" pa alla raknartexter (7 sidor)
   - Lade till skip-navigation-lank i AppShell
   - Lade till id="main-content" pa main for skip-nav-mal
   Filer skapade:
   - src/ui/web/src/hooks/useFocusTrap.ts
   - src/ui/web/src/components/Modal.tsx + Modal.css
   - src/ui/web/src/__tests__/modal.test.tsx (8 tester)
   Filer andrade:
   - src/ui/web/src/hooks/index.ts (export useFocusTrap)
   - src/ui/web/src/components/layout/AppShell.tsx + AppShell.css (skip-nav)
   - Alla 7 sidmodaler (Modal, aria-*, role)
   - 4 ovriga sidor (aria-live pa laddningsmeddelanden)
   Build: inga TS-fel. Lint: 0 nya fel. Tester: 49/49 passed. Vite build OK.

[2026-02-27] Task 4.28 DONE: Test: Tanstack Query hooks
   Skapade query-hooks.test.tsx med 27 tester som verifierar:
   - Queries: loading-state, success-state med korrekt data, error-state
   - useRunner/useCard: disabled-state när id <= 0 (fetchStatus idle)
   - Alla list-queries: useRunners, useCompetitions, useClasses, useTeams,
     useCourses, useControls, useClubs, useCards returnerar korrekt fixture-data
   - Detail-queries: useRunner, useCompetition, useCard hämtar enskild resurs
   - Create-mutations: useCreateRunner, useCreateCompetition, useCreateClass,
     useCreateTeam, useCreateClub anropar invalidateQueries med rätt queryKey
   - Update-mutations: useUpdateRunner, useUpdateCompetition, useUpdateControl
     invaliderar både list- och detail-cache
   - Delete-mutations: useDeleteRunner, useDeleteClass, useDeleteCourse
     invaliderar list-cache och tar bort detail-cache via removeQueries
   - useReadCard: mutation lyckas, returnerar korrekt cardNo, invaliderar cache
   - Error-state: useRunners och useCompetitions med 500-svar ger isError
   Filer skapade:
   - src/ui/web/src/__tests__/query-hooks.test.tsx
   Resultat: 27/27 tester PASSED.

[2026-02-27] Task 4.29 DONE: Test: Competition-sida
   Skapade competition-page.test.tsx med 18 tester som verifierar:
   - Loading state: visar "Laddar tävlingsdata…"
   - Rendering: tävlingsdetaljer (namn, datum, nolltid) och statistik (deltagare, klasser, banor, brickor)
   - Felhantering: API-fel visar felmeddelande, tom lista visar empty state
   - Edit form: öppnas via Redigera-knapp, populeras med befintlig data,
     skickar uppdatering via API, validerar tomt namn, avbryt stänger formulär,
     visar API-felmeddelande vid uppdateringsfel
   - Create form: öppnas via Ny tävling-knapp, skickar ny tävling via API,
     validerar tomt namn, Avbryt stänger formulär
   - Import/Export: renderas när tävling finns, döljs utan tävling,
     byter importformat mellan IOF och CSV
   Filer skapade:
   - src/ui/web/src/__tests__/competition-page.test.tsx
   Resultat: 18/18 tester PASSED.

[2026-02-27] Task 5.1 DONE: CMake custom command för React-build
   Implementerade CMake-integration för automatisk React-build:
   - Lade till option MEOS_BUILD_WEB (default ON) i CMakeLists.txt
   - find_program(NPM_EXECUTABLE npm) hittar npm automatiskt
   - add_custom_command kör "npm ci && npm run build" i src/ui/web/
   - OUTPUT: src/ui/web/dist/index.html (spårar om rebuild behövs)
   - DEPENDS: package.json, package-lock.json, tsconfig.json, vite.config.ts
   - add_custom_target(web-ui ALL) gör att bygget triggas automatiskt
   - add_dependencies(MeOS-server web-ui) kopplar till serverbygget
   - Varning om npm saknas; kan stängas av med -DMEOS_BUILD_WEB=OFF
   - Fixade 9 pre-existerande TS-fel som blockerade npm run build:
     * useFocusTrap.ts: non-null assertions på NodeList-element
     * AutomationsPage.tsx: optional chaining på deleteMutation.error
     * CoursesPage.tsx: non-null assertions på array-element vid swap
     * competition-page.test.tsx: tog bort oanvänd fireEvent-import
   Filer ändrade:
   - CMakeLists.txt (CMake custom command/target)
   - src/ui/web/src/hooks/useFocusTrap.ts (TS-fix)
   - src/ui/web/src/pages/AutomationsPage.tsx (TS-fix)
   - src/ui/web/src/pages/CoursesPage.tsx (TS-fix)
   - src/ui/web/src/__tests__/competition-page.test.tsx (TS-fix)
   Build: npm run build OK, dist/ producerad. Tester: 18/18 passed.

[2026-02-27] Task 5.2 DONE: Koppla restserver till React dist/
   Uppdaterade restserver.cpp att servera statiska filer från webRoot (React dist/):
   - restserver.h: lade till webRoot_ medlem, setWebRoot() och getWebRoot()
   - restserver.cpp: handleRequest() serverar index.html för GET / när webRoot satt
   - restserver.cpp: startThread() sätter set_error_handler på Restbed-service
     som fångar 404 för omatchade sökvägar och serverar statiska filer med
     korrekt MIME-typ, path traversal-skydd och SPA fallback till index.html
   - Använder befintliga readFileContents() och guessMimeType() från
     static_file_handler.h
   - server_main.cpp: lade till --web-root/-w CLI-flagga
   - server_main.cpp: fixade privat konstruktor-problem (RestServer::construct())
   - /meos-endpointen oförändrad (bakåtkompatibel)
   Filer ändrade:
   - src/net/restserver.h (webRoot_ + accessors)
   - src/net/restserver.cpp (static file serving + setWebRoot)
   - src/app/server_main.cpp (--web-root flag + construct())
   Build: g++ kompilering OK. Tester: 470/470 passed.

[2026-02-27] Task 4.30 DONE: Test: Runner-sida
   Skapade runner-page.test.tsx med 25 tester som verifierar:
   - Loading state: visar "Laddar deltagare…"
   - Rendering: deltagarlista med namn, nummerlapp, bricka, status, tid
   - Räknartext: "2 av 2 deltagare"
   - Felhantering: API-fel visar felmeddelande, tom lista visar empty state
   - Sök och filter: namn-sök, nummerlapp-sök, klass-filter, klubb-filter,
     no-match-meddelande med uppdaterad räknare
   - Sortering: standard namn asc, klick byter riktning
   - Create form: öppnas via Ny deltagare-knapp, skickar via API,
     validerar tomt namn, avbryt stänger, visar API-felmeddelande
   - Edit form: visas vid markerad rad, populeras med befintlig data,
     skickar uppdatering via API, validerar tomt namn, avbryt stänger,
     visar API-felmeddelande vid uppdateringsfel
   - Delete: tar bort deltagare via API, rensar markering
   Filer skapade:
   - src/ui/web/src/__tests__/runner-page.test.tsx
   Resultat: 25/25 tester PASSED.

[2026-02-27] Task 5.3 DONE: Skapa Dockerfile med multi-stage build
   Skapade Dockerfile med tre steg:
   - Stage 1 (web-builder): Node.js 20 Alpine, npm ci + npm run build (React)
   - Stage 2 (server-builder): Ubuntu 24.04, vcpkg + cmake/ninja, bygger
     MeOS-server med -DMEOS_USE_MYSQL=OFF -DMEOS_BUILD_WEB=OFF
   - Stage 3 (runtime): Minimal Ubuntu 24.04, bara libssl + ca-certificates,
     kopierar binär + dist/ + lang/, kör som non-root user "meos"
   - EXPOSE 2009 (default server port)
   - ENTRYPOINT ["./MeOS-server"] CMD ["--web-root", "./dist"]
   Skapade .dockerignore (exkluderar .git, build, node_modules, dist, etc.)
   Filer skapade:
   - Dockerfile
   - .dockerignore

[2026-02-27] Task 4.31 DONE: Test: Team-sida
   Skapade team-page.test.tsx med 29 tester som verifierar:
   - Loading state: visar "Laddar lag…"
   - Rendering: laglista med namn, klass, klubb, nummerlapp, status, tid, medlemmar
   - Räknartext: "1 av 1 lag"
   - Felhantering: API-fel visar felmeddelande, tom lista visar empty state
   - Sök och filter: namn-sök, nummerlapp-sök, klass-filter, klubb-filter,
     no-match-meddelande med uppdaterad räknare
   - Sortering: standard namn asc, klick byter riktning
   - Create form: öppnas via Nytt lag-knapp, skickar via API,
     validerar tomt namn, avbryt stänger, visar API-felmeddelande,
     lägg till/ta bort lagmedlemssträcka
   - Edit form: visas vid markerad rad, populeras med befintlig data inkl.
     sträckor, skickar uppdatering via API, validerar tomt namn,
     avbryt stänger, visar API-felmeddelande vid uppdateringsfel
   - Delete: tar bort lag via API, rensar markering
   - Sträckresultat: visar panel med sträcktider och löparnamn
   Filer skapade:
   - src/ui/web/src/__tests__/team-page.test.tsx
   Resultat: 29/29 tester PASSED.

[2026-02-27] Task 4.32 DONE: Test: Class-sida
   Skapade class-page.test.tsx med 24 tester som verifierar:
   - Loading state: visar "Laddar klasser…"
   - Rendering: klasslista med namn, typ, sträckor, bana (resolved via courseMap)
   - Räknartext: "3 av 3 klasser"
   - Felhantering: API-fel visar felmeddelande, tom lista visar empty state
   - Sök och filter: namn-sök, typ-filter via dropdown,
     no-match-meddelande med uppdaterad räknare
   - Sortering: standard namn asc, klick byter riktning
   - Create form: öppnas via Ny klass-knapp, skickar via API,
     validerar tomt namn, avbryt stänger, visar banalternativ (Ingen bana + banor),
     visar API-felmeddelande
   - Edit form: visas vid markerad rad, populeras med befintlig data (namn, typ,
     sträckor, bana), skickar uppdatering via API, validerar tomt namn,
     avbryt stänger, visar API-felmeddelande vid uppdateringsfel
   - Delete: tar bort klass via API, rensar markering
   Filer skapade:
   - src/ui/web/src/__tests__/class-page.test.tsx
   Resultat: 24/24 tester PASSED.

[2026-02-27] Task 4.33 DONE: Test: Course- och Control-sidor
   Skapade course-control-page.test.tsx med 47 tester som verifierar:
   CoursesPage (24 tester):
   - Loading state: visar "Laddar banor…"
   - Rendering: banlista med namn, längd, antal kontroller
   - Räknartext: "2 av 2 banor"
   - Felhantering: API-fel visar felmeddelande, tom lista visar empty state
   - Sök: namn-sök, no-match-meddelande med uppdaterad räknare
   - Sortering: standard namn asc, klick byter riktning
   - Create form: öppnas via Ny bana-knapp, skickar via API,
     validerar tomt namn, avbryt stänger, visar kontrollsekvenseditor
     med tillgängliga kontroller, visar API-felmeddelande
   - Edit form: visas vid markerad rad, populeras med befintlig data (namn,
     längd), visar kontrollsekvens från bandata, skickar uppdatering via API,
     validerar tomt namn, avbryt stänger, visar API-felmeddelande
   - Delete: tar bort bana via API, rensar markering
   ControlsPage (23 tester):
   - Loading state: visar "Laddar kontroller…"
   - Rendering: kontrolllista med namn, typ (status), koder, rogaining-poäng
   - Räknartext: "4 av 4 kontroller"
   - Felhantering: API-fel visar felmeddelande, tom lista visar empty state
   - Sök: namn-sök, kod-sök, no-match-meddelande med uppdaterad räknare
   - Sortering: standard namn asc, klick byter riktning
   - Create form: öppnas via Ny kontroll-knapp, skickar via API,
     validerar tomt namn, avbryt stänger, visar API-felmeddelande
   - Edit form: visas vid markerad rad, populeras med befintlig data (namn,
     koder), skickar uppdatering via API, validerar tomt namn,
     avbryt stänger, visar API-felmeddelande vid uppdateringsfel
   - Delete: tar bort kontroll via API, rensar markering
   Filer skapade:
   - src/ui/web/src/__tests__/course-control-page.test.tsx
   Resultat: 47/47 tester PASSED.

[2026-02-27] Task 4.34 DONE: Test: Club-, SI-, List-, Speaker-, Auto-sidor
   Skapade club-si-list-speaker-auto-page.test.tsx med 62 tester som verifierar:
   ClubsPage (20 tester):
   - Loading state, rendering, räknartext, felhantering, tom lista
   - Sök: namn-sök, no-match-meddelande med uppdaterad räknare
   - Sortering: standard namn asc, klick byter riktning
   - Create form: öppnas via Ny klubb-knapp, skickar via API,
     validerar tomt namn, avbryt stänger, visar API-felmeddelande
   - Edit form: öppnas med klubbdata, skickar uppdatering via API,
     validerar tomt namn, avbryt stänger, visar API-felmeddelande
   - Delete: tar bort klubb via API, rensar markering
   SiPage (11 tester):
   - Loading state, rendering, räknartext, felhantering, tom lista
   - Sök: bricknummer-sök, no-match-meddelande
   - Sortering: klick byter riktning
   - Stämplingsdetaljer: visas vid vald bricka
   - Läs bricka: formulär skickar via API, felmeddelande vid misslyckande
   ListsPage (8 tester):
   - Loading state, rendering av listtyp-dropdown, felhantering
   - Genererad lista vid val av listtyp
   - Resultatfiltrering: namn-sök, no-match, sortering
   SpeakerPage (9 tester):
   - Loading state, rendering av konfiguration och live-händelser
   - Felhantering, klassväljare med checkboxar, toggle
   - Tidsfönster-input, tom händelsevy, sökfält
   - Felmeddelande vid uppdateringsfel
   AutomationsPage (14 tester):
   - Loading state, rendering, räknartext, felhantering, tom lista
   - Sök: namn-sök, no-match-meddelande
   - Sortering: klick byter riktning
   - Create form: öppnas via Ny automation, skickar via API,
     avbryt stänger, visar API-felmeddelande
   - Delete: tar bort automation via API
   - Retry-knapp vid fel
   Filer skapade:
   - src/ui/web/src/__tests__/club-si-list-speaker-auto-page.test.tsx
   Resultat: 62/62 tester PASSED.

[2026-02-27] Task 4.35 DONE: Test: routing och error boundary
   Skapade routing-error-boundary.test.tsx med 21 tester som verifierar:
   Routing (14 tester):
   - Alla 11 routes renderar rätt komponent (/competition, /runners, /teams,
     /classes, /courses, /controls, /clubs, /si, /lists, /speaker, /automations)
   - Root (/) omdirigerar till /competition
   - Okänd route omdirigerar till /competition
   - AppShell layout renderas runt sidinnehåll
   Lazy loading med Suspense (1 test):
   - Visar "Laddar…" fallback medan lazy-komponent laddas, ersätts med innehåll
   RouteErrorFallback (1 test):
   - Route-komponent som kraschar visar felfallback med "Något gick fel" och retry-knapp
   ErrorBoundary (5 tester):
   - Renderar barn korrekt utan fel
   - Fångar renderfel och visar fallback med felmeddelande
   - Visar generiskt meddelande vid tomt Error
   - Återhämtar sig vid klick på "Försök igen"
   - Loggar fel till konsolen
   Filer skapade:
   - src/ui/web/src/__tests__/routing-error-boundary.test.tsx
   Resultat: 21/21 tester PASSED.

[2026-02-27] Task 4.36 DONE: Test: accessibility (a11y-audit)
   Skapade a11y-audit.test.tsx med 11 tester som verifierar:
   - Installerade vitest-axe (axe-core wrapper för Vitest)
   - Konfigurerade expect.extend med toHaveNoViolations-matcher
   - Inaktiverade color-contrast-regel (ej tillförlitlig i jsdom)
   - Varje sida renderas med MSW-mockade API-svar, väntar på loaded state
   - axe-core körs på renderat DOM för varje sida
   Sidor testade (11):
   CompetitionPage, RunnersPage, TeamsPage, ClassesPage, CoursesPage,
   ControlsPage, ClubsPage, SiPage, ListsPage, SpeakerPage, AutomationsPage
   Filer skapade:
   - src/ui/web/src/__tests__/a11y-audit.test.tsx
   Resultat: 11/11 tester PASSED.

[2026-02-27] Task 5.4 DONE: Skapa docker-compose.yml
   Skapade docker-compose.yml med två tjänster:
   - meos: bygger från Dockerfile, exponerar port 2009, volym meos-data
     för tävlingsdata, environment-variabler för MySQL-anslutning,
     depends_on mysql med healthcheck
   - mysql: MySQL 8.0, skapar databas/användare via env-vars,
     volym mysql-data för persistens, healthcheck med mysqladmin ping
   Konfiguration:
   - Nätverk: meos-net (bridge driver) kopplar ihop tjänsterna
   - Volymer: meos-data (tävlingsdata), mysql-data (databas)
   - restart: unless-stopped på båda tjänster
   Validering: docker compose config passerar utan varningar
   Filer skapade:
   - docker-compose.yml

[2026-02-27] Task 5.6 DONE: E2E: setup och smoke-tester
   Konfigurerade Playwright (TypeScript) i src/ui/web/:
   - Installerade @playwright/test och Chromium headless browser
   - Skapade playwright.config.ts med webServer-integration (Vite dev server)
   - Konfigurerat: fullyParallel, retries i CI, html/github reporter,
     trace on-first-retry, screenshot only-on-failure
   Smoke-tester i e2e/smoke.spec.ts (18 tester):
   - App laddas och visar MeOS-header
   - Root (/) omdirigerar till /competition
   - Navigeringsfält synligt med alla 11 länkar
   - Okänd route omdirigerar till /competition
   - Alla 11 sidor renderar (lazy-loaded via Suspense)
   - Navigation mellan sidor fungerar (klick, URL-ändring)
   - Aktiv nav-länk markeras med active-klass
   - Skip-nav-länk finns för tillgänglighet
   Tillagda npm-scripts: test:e2e, test:e2e:ui
   Uppdaterade .gitignore med Playwright-artefakter
   Filer skapade:
   - src/ui/web/playwright.config.ts
   - src/ui/web/e2e/smoke.spec.ts
   Filer ändrade:
   - src/ui/web/package.json (scripts + devDependencies)
   - src/ui/web/.gitignore (Playwright-artefakter)
   Resultat: 18/18 tester PASSED.

[2026-02-27] Task 5.7 DONE: E2E: komplett tävlingsflöde
   Skapade competition-flow.spec.ts med 5 Playwright E2E-tester som verifierar:
   Komplett flöde (1 test):
   - Skapa tävling → se klass → registrera löpare → läs SI-bricka → visa resultat
   - Använder Playwright route-mocking för alla API-endpoints
   - Verifierar att data flödar mellan sidor (runner syns i resultat efter brickavläsning)
   Redigera tävling (1 test):
   - Öppnar redigering, byter namn, sparar → verifierar uppdaterat namn
   Skapa bana (1 test):
   - Tom banlista → ny bana → verifierar att banan visas
   Tävlingsstatistik (1 test):
   - Verifierar att antal deltagare/klasser/banor/brickor visas korrekt
   Flöde med flera löpare (1 test):
   - Registrerar 2 löpare → läser 2 SI-brickor → verifierar båda i resultatlistan
   Filer skapade:
   - src/ui/web/e2e/competition-flow.spec.ts
   Resultat: 5/5 tester PASSED.

[2026-02-27] Task 5.10 DONE: Test: Docker build lyckas
   Lade till docker-build job i .github/workflows/build.yml:
   - Set up Docker Buildx (docker/setup-buildx-action@v3)
   - Build Docker image med docker/build-push-action@v6 (push: false, load: true)
   - GHA build cache (cache-from/cache-to: type=gha)
   - Startar container (docker run -d, port 2009)
   - Väntar på container ready (polling /meos endpoint, max 60s)
   - Verifierar health endpoint (HTTP status 2xx/3xx)
   - Cleanup: stoppar och tar bort container (always())
   Filer ändrade:
   - .github/workflows/build.yml (ny docker-build job)
   Validering: YAML-struktur verifierad, inga tab-tecken, alla actions korrekt refererade.

[2026-02-27] Task 5.5 DONE: Dokumentera ny arkitektur i README
   Uppdaterade README.md med fullständig dokumentation:
   - Arkitekturöversikt: headless C++ server + React frontend
   - Komponenttabell (server, GUI, web frontend, databas)
   - Build targets (MeOS-server, MeOS, MeOS-test)
   - Headless vs GUI-jämförelsetabell
   - REST API-översikt med alla endpoints
   - Bygginstruktioner per plattform (Linux, macOS, Windows)
   - CMake-alternativ (MEOS_USE_MYSQL, MEOS_BUILD_WEB)
   - Web frontend standalone-instruktioner (dev, build, test, e2e)
   - Docker: single container och docker-compose full stack
   Filer ändrade:
   - README.md

[2026-02-27] Task 5.8 DONE: E2E: import och export
   Skapade import-export.spec.ts med 5 Playwright E2E-tester som verifierar:
   - Import IOF XML-fil → löpare dyker upp på Deltagare-sidan
   - Import CSV-fil → löpare dyker upp på Deltagare-sidan
   - Export IOF XML → korrekt filnamn (export.xml) och statusmeddelande
   - Export CSV → korrekt filnamn (export.csv) och statusmeddelande
   - Round-trip: importera IOF XML → exportera → verifiera att exporterad fil
     innehåller importerade löpare (Anna Svensson, Erik Johansson, Maria Karlsson)
   Alla tester använder Playwright route-mocking för API-endpoints.
   Import-mock uppdaterar runner-state så att efterföljande navigation visar data.
   Filer skapade:
   - src/ui/web/e2e/import-export.spec.ts
   Resultat: 5/5 tester PASSED.

[2026-02-27] Task 5.9 DONE: E2E: parallella klienter
   Skapade parallel-clients.spec.ts med 4 Playwright E2E-tester som verifierar:
   - Löpare tillagd i klient A syns i klient B efter navigation (delat mock-state)
   - Tävlingsuppdatering i klient A synlig i klient B efter reload
   - Flera löpare från olika klienter sammanfogas korrekt (båda synliga efter reload)
   - Bana skapad i klient A visas i klient B vid navigation
   Två separata BrowserContext per test delar mutable mock-state via route-interceptors.
   Filer skapade:
   - src/ui/web/e2e/parallel-clients.spec.ts
   Resultat: 4/4 tester PASSED.

[2026-02-27] Task 5.11 DONE: Test: docker-compose full stack
   Lade till docker-compose-test job i .github/workflows/build.yml:
   - docker compose up -d --build --wait (med 180s timeout)
   - Polling: väntar på MeOS /meos endpoint (max 45 iterationer × 4s)
   - Verifierar app serverar frontend (HTTP 2xx/3xx)
   - Verifierar MySQL hälsa via mysqladmin ping
   - Graceful shutdown: docker compose down --timeout 30, verifierar inga containers kvar
   Filer ändrade:
   - .github/workflows/build.yml (ny docker-compose-test job)

[2026-02-27] Task 5.12 DONE: Konfigurera CI quality gates
   Skapade web-quality job i .github/workflows/build.yml med komplett kvalitetspipeline:
   - TypeScript type check (tsc --noEmit)
   - ESLint linting
   - Vitest enhetstester med coverage (v8 provider)
     - JUnit XML-rapport (reports/vitest-junit.xml) i CI
     - Coverage: lcov + json-summary + text
     - Tröskelvärden: 70% lines/functions/statements, 65% branches (baseline)
   - Playwright E2E-tester med JUnit-rapport i CI
   - Upload test reports som CI-artifacts (14 dagars retention)
   - Upload Playwright screenshots vid failure (7 dagars retention)
   C++ tester: lade till --output-junit för CTest JUnit XML i build-linux job
   Tillagda npm-scripts: test:coverage, typecheck
   Installerade: @vitest/coverage-v8
   Filer ändrade:
   - .github/workflows/build.yml (web-quality job, build-linux JUnit output)
   - src/ui/web/vitest.config.ts (coverage + JUnit reporters)
   - src/ui/web/playwright.config.ts (JUnit reporter i CI)
   - src/ui/web/package.json (test:coverage, typecheck scripts, devDep)
   - src/ui/web/.gitignore (/reports/)
   Validering: 313/313 Vitest-tester PASSED, coverage thresholds PASSED,
   TypeScript typecheck PASSED.
